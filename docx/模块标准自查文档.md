# 模块标准自查文档

> 以模块一「HTML 基础」修复后的质量基线为基准，抽象出可复用的模块质量标准，供其它模块在新建/重构时对照自查，确保全站学习体验的一致性与有效性。

***

## 一、文档目的

本文件解决一个具体问题：**如何判断一个学习模块是否"达标"？**

模块一此前存在三类典型缺陷——评估环节形同虚设、知识呈现静态化、内容组织学科导向。经修复后，模块一在「以测促学 / 动态反馈 / 任务导向」三个维度上建立了可参照的基线。本文件把该基线抽象成 7 个自查维度 + 1 张勾选清单，使其它模块无需重复摸索即可对齐质量。

**适用对象**：所有 `src/modules/*.ts` 学习模块（模块二 CSS、模块三 JS …… 及后续新增模块）。

**使用方式**：模块开发/重构完成后，逐项对照第三章维度与第四章清单打勾；任一维度不达标即视为该模块未完成质量门禁。

***

## 二、模块一基线概览（实例对照）

修复后的模块一达成了以下量化指标，作为其它模块的**下限参考**（不是上限，鼓励超越）：

| 维度                  | 模块一实例值 | 说明                                      |
| ------------------- | ------ | --------------------------------------- |
| 知识点数量               | **26** | order 1–26 连续无跳号                        |
| 面试题数量               | **16** | 覆盖文档流/盒模型/表单验证/meta/无障碍/语义化/srcset/场景题等 |
| 小测题数量               | **15** | 含【记忆】【理解】【应用】【对比】【场景】【综合】梯度标注           |
| 综合实战项目              | **2**  | KP22 搭建博客文章页、KP23 带验证注册表单               |
| 沙盒断言 checks         | **17** | 9 + 8，分布在 2 个实战项目沙盒                     |
| 知识点速查表              | **1**  | KP25                                    |
| 类型检查 `tsc --noEmit` | **通过** | exit 0                                  |
| 模块测试                | **通过** | html-fundamentals.test.ts 全绿            |

**关键文件锚点**：

- 模块数据：[src/modules/html-fundamentals.ts](../src/modules/html-fundamentals.ts)
- 沙盒类型：[src/lib/types.ts](../src/lib/types.ts) `SandboxData` / `SandboxCheck`
- 沙盒组件：[src/components/interactive/Sandbox.tsx](../src/components/interactive/Sandbox.tsx)
- 模块测试：[src/test/html-fundamentals.test.ts](../src/test/html-fundamentals.test.ts)

***

## 三、自查维度

每个维度包含：**① 标准要求 → ② 模块一实例 → ③ 检查方法 → ④ 常见缺陷**。

### 维度 A：知识点结构与编排

**① 标准要求**

- A1. `points` 数组的 `order` 字段从 1 连续递增至 N，**无重复、无跳号、无小数**。
- A2. 每个知识点具备 `title` / `difficulty`(1–5) / `blocks` 三要素。
- A3. 难度分布合理：以 1–3 为主，进阶内容(4–5)不超过总量的 30%。
- A4. 新增/重构的知识点显式标注 `isNew: true`，便于学习者识别更新。
- A5. `blocks` 内 `id` 遵循 `p{order}-{n}` 命名规范，全模块唯一。

**② 模块一实例**

26 个知识点 order 1–26 严格连续；KP22/KP23 实战项目标注 `isNew: true`；难度集中在 1–2，仅个别 API 章节为 3。

**③ 检查方法**

```bash
# 提取所有 order 值，人工核对连续性
# （用 IDE 正则搜索 ^\s+order:\s+(\d+),）
```

或在模块测试中断言：

```ts
it('知识点序号应从 1 到 N 连续', () => {
  const orders = someModule.points.map((p) => p.order)
  for (let i = 0; i < orders.length; i++) {
    expect(orders[i]).toBe(i + 1)
  }
})
```

**④ 常见缺陷**

- 插入新知识点后未重排后续 order，导致跳号/重复（模块一曾出现 order 21 / 21.5 / 22 冲突）。
- 用小数 order（如 21.5）作为临时插入，破坏连续性约定。

***

### 维度 B：评估环节有效性（以测促学）

**① 标准要求**

- B1. **面试题 ≥ 30 题**，覆盖该模块所有核心考点，无高频考点遗漏。
- B2. **小测题 ≥ 20 题**，且分布在模块末尾的独立 QuizCard 知识点。
- B3. 题目具备**梯度**，能区分"记忆 / 理解 / 应用"三个层次：
  - 记忆层：直问定义、标签类型；
  - 理解层：概念辨析、原理说明；
  - 应用层：场景判断、代码选型、排错。
- B4. 面试题至少包含 **2 道场景题**（"遇到 X 问题你怎么排查/重构"）与 **2 道对比题**（易混淆点 A vs B）。
- B5. 每道题的 `explanation` / `content` 不仅给结论，还解释"为什么"，指向易错点。
- B6. 考点覆盖率自查：列出模块所有核心知识点，逐个标注是否有对应题目，无盲区。

**② 模块一实例**

- 面试题 16 题：Q14 场景题（页面卡顿排查）、Q15 场景题（div 导航重构）、Q8 对比题（标准/IE 盒模型）、Q16 对比题（`<b>`/`<strong>` 等）。
- 小测题 15 题，题干前缀标注【理解】【应用】【对比】【场景】【综合】。
- 考点覆盖：文档流、盒模型、meta、viewport、语义化、defer/async、无障碍 POUR、ARIA、表单验证、srcset、空元素等均有对应题目。

**③ 检查方法**

```bash
# 统计面试题数量（匹配 Q{n}: 前缀）
# 统计小测题数量（匹配 ^\s+question:）
# 人工核对：是否存在"学了但没考"的知识点
```

**④ 常见缺陷**

- 题目全是直问定义，无法区分背书与理解（缺应用层）。
- 高频考点（如模块一的"文档流""表单验证"）无对应题目。
- 题目集中在模块末尾，章节学完无即时检验（理想态：每章末可加微测验，至少模块级总测验要全覆盖）。

***

### 维度 C：动态反馈闭环（沙盒断言）

**① 标准要求**

- C1. `SandboxData` 类型支持 `checks?: SandboxCheck[]` 字段（已由 types.ts 全局提供，各模块直接使用）。
- C2. `Sandbox.tsx` 渲染"任务检查清单"面板，对学习者代码**实时**跑断言并显示通过/未通过 + 中文教学提示（已由组件全局提供）。
- C3. 模块内**任务型练习沙盒**（尤其是实战项目）必须配置 `checks`，至少 5 条。**自由探索型沙盒**（hint 为"修改 X 观察 Y"的演练场）可不配 checks，因其教学目的是自由试错而非任务达标。区分依据：沙盒是否设定了明确的"完成目标"。
- C4. 每条 check 三要素齐全：`description`（任务要求）/ `pattern`（正则）/ `hint`（未通过时的教学引导）。
- C5. `pattern` 必须在沙盒中可正确编译（避免正则语法错误导致永远不通过）。
- C6. 断言不只是"语法存在性"检查，应体现**教学意图**（如"必须写 alt"对应无障碍教学点）。
- C7. 全部通过时显示完成态，给学习者正反馈。

**② 模块一实例**

- KP22 博客文章页沙盒：9 条 checks（charset / viewport / header / nav / main / article / img+alt / aside / footer）。
- KP23 注册表单沙盒：8 条 checks（form / text+required / email+required / password+minlength / select / button\[type=submit] / label\[for] / aria-required）。
- 每条 hint 指出"该怎么做 + 为什么"，例如 img+alt 的 hint 解释"无 alt 的图片对视障用户不存在，违反 WCAG 可感知原则"。

**③ 检查方法**

```bash
# 统计 checks 数量（匹配 ^\s+pattern:）
# 人工核对：每个配置了 checks 的沙盒，其 pattern 是否能在预期代码上通过、在错误代码上失败
```

**类型字段速查**：

```ts
export interface SandboxCheck {
  description: string  // 任务要求（展示给学习者）
  pattern: string      // 正则字符串，命中即通过
  flags?: string       // 默认 'i'
  hint: string         // 未通过时的教学提示
}
```

**④ 常见缺陷**

- 沙盒只支持"编辑-预览"，写错只能看浏览器原生报错，无教学反馈（模块一修复前的状态）。
- `pattern` 写得过严或过松：过严导致正确写法被判失败，过松导致随便写都通过。
- 只有语法检查没有语义检查（如只查"有没有 `<img>`"而不查"有没有 alt"）。

***

### 维度 D：任务导向内容组织（综合实战项目）

**① 标准要求**

- D1. 模块内至少 **1 个**（建议 2 个）综合实战知识点，放在该模块核心章节学完之后。
- D2. 实战项目必须**贯穿多个章节**的知识点，扮演"拼装图纸"角色，而非单点练习。
- D3. 每个实战项目配 `checks` 断言沙盒（接入维度 C）。
- D4. 沙盒提供**初始骨架代码**（带注释占位），降低冷启动门槛。
- D5. 项目前后有 `callout` 说明"为什么这个练习重要"与"实战后的反思"，连接工程思维。
- D6. 项目命名体现**真实开发场景**（如"搭建博客文章页""带验证的注册表单"），而非"练习 1"。

**② 模块一实例**

- KP22「搭建博客文章页」：串联 head 元信息 + 语义化骨架 + 多媒体无障碍，9 条断言，含 `callout variant="tip"` 说明练习价值。
- KP23「带验证注册表单」：串联表单控件 + 原生验证 + 无障碍 label 关联，8 条断言，含 `callout variant="warning"` 反思"前端验证的边界"（衔接后端校验）。
- 两者均提供带 `<!-- -->` 占位注释的初始骨架。

**③ 检查方法**

```bash
# 搜索实战项目知识点（title 含"实战"/"综合"）
# 核对每个实战沙盒是否配置 checks、是否有初始骨架、是否有 callout
```

**④ 常见缺陷**

- 知识按学科分类（结构→语义→多媒体→表单→API），学完仍不会"从零搭页面"。
- 没有贯穿多章的渐进式项目，知识点是离散积木缺拼装图纸。
- 实战项目变成"照抄示例"，无断言引导，无法定位薄弱环节。

***

### 维度 E：辅助学习资源

**① 标准要求**

- E1. 模块末尾含 1 个**知识点速查表**（`visualizationType: 'cheatsheet'` 或等价表格），浓缩核心 API/标签/属性。
- E2. 关键概念处使用 `callout`（`tip` / `warning` / `info`）强调易错点、最佳实践、边界条件。
- E3. 首个知识点有 `lead: true` 的引导段落，说明本章学什么、为什么学。
- E4. 代码示例遵守项目设计规范（canvas-mid 代码区、hairline 边框、等宽字体）。

**② 模块一实例**

- KP25 知识点速查表。
- 多处 callout：如表单章节的"无障碍表单要点"(tip)、KP23 的"前端验证边界"(warning)。
- 每章首段 `lead: true` 引导。

**③ 检查方法**

```bash
# 搜索 callout 使用密度（匹配 type: 'callout'）
# 确认存在 cheatsheet / 速查表知识点
```

**④ 常见缺陷**

- 全文平铺直叙，无重点强调，学习者抓不住易错点。
- 缺速查表，复习时无法快速定位。

***

### 维度 F：类型与工程质量

**① 标准要求**

- F1. `npx tsc --noEmit` 通过（exit 0），无类型错误。
- F2. 存在对应模块测试 `src/test/{module}.test.ts`，且全绿。
- F3. 测试覆盖：知识点数量、order 连续性、每个知识点三要素、可视化组件元数据。
- F4. 模块数据严格符合 `ModuleData` 类型（`points` / `blocks` / `data` 结构正确）。
- F5. 新增 `visualizationType` 时同步登记到 `visualizationMeta`（避免 types.test.ts 漂移）。
- F6. 不引入与模块无关的全局类型变更（避免污染其它模块）。
- F7. 沙盒 `language` 值须在 `SandboxData.language` 类型联合（`'js' | 'html' | 'css' | 'typescript'`）内，新增语言须同步扩展类型联合（避免对象字面量多余属性报错）。
- F8. 可视化组件多样性：全模块使用 ≥ 8 种不同 `visualizationType`（避免单一组件堆砌，提升学习体验）。
- F9. 测试断言阈值与标准一致：模块测试中的数量/比例断言（面试题 ≥ 30、小测题 ≥ 20、checks ≥ 5、进阶占比 ≤ 30%、场景题 ≥ 2、对比题 ≥ 2）须与第四章清单保持一致，避免"标准说 30、测试只测 12"的阈值漂移。

**② 模块一实例**

- `tsc --noEmit` exit 0。
- `html-fundamentals.test.ts` 14/14 通过（含 26 知识点、order 连续、三要素校验）。
- 注：`types.test.ts` 的 `visualizationMeta` (105 vs 173) 失败为**预先存在**的全局漂移，与模块一无关，需单独治理。

**③ 检查方法**

```bash
npx tsc --noEmit
npx vitest run src/test/{module}.test.ts --reporter=dot
```

**④ 常见缺陷**

- 新增知识点后忘更新测试中的 `toHaveLength(N)`，导致测试红。
- 新增可视化类型未登记 `visualizationMeta`，触发 types.test.ts 失败。
- order 用小数临时插入导致连续性测试失败。

***

### 维度 G：阅读习惯规范

**① 标准要求**

- G1. 面试题 `content` 采用结构化排版：要点间用空行分隔（`\n\n`），避免单段长文堆砌，便于 flashcard 模式快速扫读。
- G2. 段落优先用「主标题 + 缩进列表」结构（如「原因：」「优化：」分节，每节下用 `-` 列举），而非平铺整段。
- G3. 列表项保持平行结构（同层级项句式一致，如全以动词开头或全以名词开头），避免长短不一、句式杂糅。

**② 模块一实例**

- 面试题答案统一「结论先行 → 分点展开 → 场景落地」三段式。
- callout 文案简洁，一图胜千言处配 `visualizationType`。

**③ 检查方法**

```bash
# 统计面试题答案中 \n\n 出现次数（应 ≥ 题数，即每题至少一段分隔）
# 人工抽查 3-5 题，确认分节清晰、列表平行
```

**④ 常见缺陷**

- 答案写成一整段，flashcard 翻面后无法快速定位要点。
- 列表项句式杂糅（有的「因为…」有的「原因是…」），阅读节奏被打断。

***

## 四、自查清单（勾选式）

> 模块开发/重构完成后，逐项打勾。**任一未勾选 = 未过质量门禁。**

### A. 知识点结构

- [ ] A1. `order` 1..N 连续无重复无跳号无小数
- [ ] A2. 每个知识点有 `title` / `difficulty` / `blocks`
- [ ] A3. 难度分布合理（1–3 为主）
- [ ] A4. 新增知识点标注 `isNew: true`
- [ ] A5. block `id` 符合 `p{order}-{n}` 且全模块唯一

### B. 评估环节

- [ ] B1. 面试题 ≥ 30 题
- [ ] B2. 小测题 ≥ 20 题
- [ ] B3. 题目含记忆/理解/应用三梯度
- [ ] B4. 面试题含 ≥ 2 场景题 + ≥ 2 对比题
- [ ] B5. 每题 `explanation` 解释"为什么"
- [ ] B6. 核心考点全覆盖（无"学了没考"盲区）

### C. 动态反馈闭环

- [ ] C1. 使用 `SandboxData.checks`（类型已就绪）
- [ ] C2. 关键沙盒配置 `checks`（≥ 5 条）
- [ ] C3. 每条 check 三要素齐全（description/pattern/hint）
- [ ] C4. `pattern` 经实测：正确代码通过、错误代码失败
- [ ] C5. 断言体现教学意图（非纯语法检查）
- [ ] C6. 全部通过有完成态反馈

### D. 任务导向实战

- [ ] D1. 至少 1 个综合实战知识点（建议 2 个）
- [ ] D2. 项目贯穿多章节知识点
- [ ] D3. 配 `checks` 断言沙盒
- [ ] D4. 提供带注释占位的初始骨架
- [ ] D5. 前后有 `callout`（练习价值 + 反思）
- [ ] D6. 命名体现真实开发场景

### E. 辅助资源

- [ ] E1. 模块末尾有知识点速查表
- [ ] E2. 关键概念有 `callout` 强调
- [ ] E3. 首知识点有 `lead: true` 引导段
- [ ] E4. 代码示例符合设计规范
- [ ] E5. 综合能力类模块须覆盖非技术维度（软技能/业务理解/产品思维），不应仅有技术八股

### F. 工程质量

- [ ] F1. `tsc --noEmit` 通过
- [ ] F2. 模块测试存在且全绿
- [ ] F3. 测试覆盖数量/order/三要素
- [ ] F4. 数据符合 `ModuleData` 类型
- [ ] F5. 新增 `visualizationType` 已登记 `visualizationMeta`
- [ ] F6. 未引入无关全局类型变更
- [ ] F7. 沙盒 `language` 值在 `SandboxData.language` 类型联合内（`'js' | 'html' | 'css' | 'typescript'`），新增语言须同步扩展类型联合（避免对象字面量多余属性报错）
- [ ] F8. 可视化组件多样性：全模块使用 ≥ 8 种不同 `visualizationType`（避免单一组件堆砌，提升学习体验）
- [ ] F9. 测试断言阈值与标准一致：模块测试中的数量/比例断言（面试题 ≥ 30、小测题 ≥ 20、checks ≥ 5、进阶占比 ≤ 30%、场景题 ≥ 2、对比题 ≥ 2）须与第四章清单保持一致，避免"标准说 30、测试只测 12"的阈值漂移
- [ ] F10. 单引号字符串内部不得包含未转义单引号（`'...\'...'` 会导致 oxc 解析器误判字符串终止，tsc 可能通过但 vitest/vite 转译失败）；含代码示例的 `detailedAnswer` 须检查嵌套引号
- [ ] F11. `visualizationMeta` 键数量断言使用 `>= expectedTypes.length` 而非精确匹配，避免新增可视化类型后测试回归

### G. 阅读习惯规范

- [ ] G1. 面试题答案用 `\n\n` 分段，flashcard 可快速扫读
- [ ] G2. 段落用「标题 + 列表」结构，非单段长文
- [ ] G3. 列表项句式平行（同层级一致）

***

## 五、应用流程（如何把本标准用到新模块）

1. **新建模块骨架**：复制 `html-fundamentals.ts` 的结构（顶部 import、`ModuleData` 形状），改 `number/title/slug/stage/icon`。
2. **编写知识点**：按 `order` 1..N 顺序编写，遵循 A 系列标准。
3. **穿插实战项目**：在核心章节后插入综合实战 KP，配置 `checks` 沙盒（D + C 标准）。
4. **编写评估题**：先列出全部核心考点，再为每个考点配题，确保 B6 无盲区；题目前缀标注梯度。
5. **补速查表与 callout**：模块末尾加速查表，关键处加 callout（E 标准）。
6. **编写模块测试**：参照 `html-fundamentals.test.ts`，覆盖数量/order 连续/三要素。测试中的数量/比例断言须与第四章清单阈值一致（F9），避免"标准说 30、测试只测 12"。
7. **跑质量门禁**：
   ```bash
   npx tsc --noEmit
   npx vitest run src/test/{module}.test.ts --reporter=dot
   ```
8. **对照第四章清单逐项打勾**，未勾项回到对应维度修复。
9. **登记可视化类型**：若引入新 `visualizationType`，同步更新 `visualizationMeta` 与 `types.test.ts`。

***

## 六、模块检查实录

> 每完成一个模块的自查与修复，在此追加一节，记录「检查项 → 发现 → 修复 → 验证」，供后续模块参照。

### 6.1 模块 24：数据结构与前端算法（`data-structure-algorithm.ts`）

**检查日期**：2026-06-27

#### 检查结果（按维度）

| 维度 | 检查项 | 状态 | 说明 |
|------|--------|------|------|
| A | A1-A5 知识点结构 | ✅ 通过 | 30 个知识点，order 1..30 连续无跳号；难度分布 1×5 + 2×11 + 3×14，进阶占比 0%（远低于 30% 上限）；block id 符合 `p{order}-{n}` 规范 |
| B | B1 面试题题量 | ❌→✅ 已修复 | 原 24 题 < 30，新增 Q25-Q30 共 6 题（归并/快排对比、二分边界、位运算、千万级渲染、TTL+LRU、综合拆解法） |
| B | B2 小测题题量 | ✅ 通过 | 20 题，满足 ≥ 20 |
| B | B3 题目梯度 | ✅ 通过 | 小测题标注记忆/理解/应用/对比/场景/综合梯度 |
| B | B4 场景/对比题 | ❌→✅ 已修复 | 原无前缀标注，现补 6 个【对比题】+ 4 个【场景题】前缀，满足 ≥ 2 + ≥ 2 |
| B | B5-B6 考点覆盖 | ✅ 通过 | 覆盖栈/队列/链表/树/图/排序/查找/DP/堆/Trie/并查集等核心考点 |
| C | C1-C6 动态反馈 | ✅ 通过 | 使用算法专用可视化（codestepper/sorting-race-arena/binary-tree-walker/bfs-path-finder 等）替代标准沙盒 checks，符合算法模块特性 |
| D | D1-D6 任务导向 | ✅ 通过 | 含综合实战 KP，贯穿多章知识，配 callout |
| E | E1-E4 辅助资源 | ✅ 通过 | 末尾速查表 + 关键处 callout + lead 引导段 |
| F | F1 tsc | ✅ 通过 | `npx tsc --noEmit` exit 0 |
| F | F2 测试全绿 | ✅ 通过 | 20/20 通过 |
| F | F3 测试覆盖 | ✅ 通过 | 覆盖数量/order/三要素/可视化元数据 |
| F | F4-F6 类型与全局 | ✅ 通过 | 符合 ModuleData 类型，未引入无关全局变更 |
| F | F8 多样性 | ✅ 通过 | 12 种 visualizationType（≥ 8） |
| F | F9 断言阈值 | ❌→✅ 已修复 | 原断言面试题 ≥ 20（低于标准 30），现收紧至 ≥ 30 并新增场景题/对比题计数断言 |
| G | G1-G3 阅读习惯 | ✅ 通过 | 面试题答案均用 `\n\n` 分段 + 标题列表结构，flashcard 可快速扫读 |

#### 修复明细

1. **B1 面试题扩至 30 题**：在 `data-structure-algorithm.ts` 面试题 accordion 中追加 Q25-Q30：
   - Q25【对比题】归并 vs 快排
   - Q26 二分查找边界条件
   - Q27 位运算技巧与前端场景
   - Q28【场景题】千万级数据渲染优化
   - Q29【场景题】带 TTL 的 LRU 缓存设计
   - Q30【综合】面试算法题拆解法

2. **B4 场景/对比题前缀**：为现有面试题补充类型标注——Q5/Q9/Q13/Q14/Q24/Q28/Q29 标【场景题】（7 题），Q6/Q8/Q10/Q19/Q21/Q22/Q25 标【对比题】（7 题），均 ≥ 2。

3. **F9 测试断言收紧**：`data-structure-algorithm.test.ts` 中：
   - 面试题断言从 `>= 20` 收紧至 `>= 30`
   - 新增场景题计数断言（`>= 2`）与对比题计数断言（`>= 2`）
   - 标题正则放宽为 `/^Q\d+(\s+【[^】]+】)?[:：]/` 以兼容类型前缀

4. **G 维度新增**：本次检查发现原标准缺「阅读习惯」维度，补入维度 G（G1 分段 / G2 结构 / G3 平行），并同步更新引言「7 个自查维度」与自查清单。

#### 验证

```bash
npx tsc --noEmit                                    # exit 0
npx vitest run src/test/data-structure-algorithm.test.ts --reporter=dot
# Test Files  1 passed (1)
# Tests       20 passed (20)
```

#### 关键决策记录

- **算法模块可视化策略**：模块 24 未使用标准 `SandboxData.checks`，而是采用算法专用可视化组件（`sorting-race-arena`、`binary-tree-walker`、`bfs-path-finder`、`codestepper` 等）。理由：算法学习重在「过程可视化」而非「代码正误检查」，专用组件比通用沙盒 checks 更契合教学目标。此为 C 维度的合理变体，不视为不符合项。

### 6.2 模块 25：面试八股与综合能力（`interview-prep.ts`）

**检查日期**：2026-06-27

#### 检查结果（按维度）

| 维度 | 检查项 | 状态 | 说明 |
|------|--------|------|------|
| A | A1-A5 知识点结构 | ❌→✅ 已修复 | 原 15 个知识点，新增 KP16 软技能与协作、KP17 业务理解与产品思维，现 17 个知识点，order 1..17 连续；block id 符合 `iv-p{order}-{n}` 规范 |
| A | 可视化资源分配均衡 | ❌→✅ 已修复 | 原 CSS/浏览器/安全仅用对比表，现 KP5 浏览器追加 codestepper 渲染流程演示、KP6 安全追加 codestepper XSS 攻击链演示，KP2 CSS 追加 quiz-engine 5 题 |
| B | 各 domain 题量平衡 | ❌→✅ 已修复 | 原 JS 15/React 13/安全 6/工程化 6/测试 4 差距大；现 JS 18/CSS 6/React 9/网络 9/安全 6，核心 domain 均 ≥ 5，JS/React/网络 ≥ 8 |
| C | C1-C6 动态反馈 | ❌→✅ 已修复 | KP5/KP6 从仅对比表升级为 codestepper + quiz-engine 双组件；KP16 软技能用 accordion 场景折叠；KP17 业务理解用 comparetable + quiz-engine |
| D | D1-D6 任务导向 | ✅ 通过 | KP15 模拟面试计时器 + progress-dashboard + 综合测验覆盖多 domain |
| E | E1-E4 软技能/业务维度 | ❌→✅ 已修复 | 原模块仅技术八股，新增 KP16（沟通协作/冲突解决/向上管理）+ KP17（业务理解/产品思维/数据驱动） |
| F | F1 tsc | ✅ 通过 | `npx tsc --noEmit` exit 0 |
| F | F2 测试全绿 | ✅ 通过 | 30/30 通过（新建 interview-prep.test.ts） |
| F | F8 多样性 | ✅ 通过 | 使用 interview-quiz-engine / comparetable / codestepper / accordion / mock-interview-timer / progress-dashboard / system-design-board / quiz 等 8 种可视化（≥ 6） |
| F | F9 断言阈值 | ✅ 通过 | 新测试含 domain 题量平衡断言（核心 ≥ 5，JS/React/网络 ≥ 8）、KP16/KP17 存在性断言、progress-dashboard 数据一致性断言 |
| G | G1-G3 阅读习惯 | ✅ 通过 | quiz-engine 题目含 detailedAnswer 用 `\n\n` 分段 |

#### 修复明细

1. **知识点覆盖补全（A 维度）**：
   - KP5 浏览器：追加 `codestepper` 渲染流程演示（DOM→CSSOM→Render Tree→Layout→Paint→Composite 共 6 步）+ `interview-quiz-engine` 5 题（渲染流程/重排重绘/事件循环/垃圾回收/V8 JIT）
   - KP6 安全：追加 `codestepper` XSS 攻击链演示（构造 payload→注入→执行→窃取→防御 5 步）+ `interview-quiz-engine` 5 题（XSS 三型/CSRF/CSP/HttpOnly/同源策略）
   - KP2 CSS：追加 `interview-quiz-engine` 5 题（Flex 深入/Grid vs Flex/选择器优先级/响应式/动画性能）
   - KP3 框架：追加 5 题达 8 题（Vue computed/watch/methods/HOC-render-props-hooks/Vue3 reactivity）
   - KP4 网络：追加 5 题达 8 题（HTTP 缓存/CORS/TCP 三次握手/HTTP2 vs HTTP3/WebSocket）

2. **STAR 差异化（D 维度）**：KP8 追加 ToB/ToC/基建三列对比表，覆盖业务场景/核心指标/复杂度/成功标准/常见坑五个维度。

3. **新增 KP16 软技能与协作（E 维度）**：
   - `accordion` 折叠面板：沟通协作/冲突解决/向上管理/跨团队推动 四场景
   - `interview-quiz-engine`：5 道软技能面试题（分歧处理/推动落地/跨部门协作/向上管理/技术选型说服）
   - `callout`：软技能自评清单

4. **新增 KP17 业务理解与产品思维（E 维度）**：
   - `comparetable`：业务理解/产品思维/数据驱动/技术-业务取舍/用户视角 五维度对比
   - `interview-quiz-engine`：5 道业务思维题（功能价值/MVP 设计/数据驱动决策/技术债取舍/用户反馈处理）
   - `callout`：业务思维自评清单

5. **progress-dashboard 数据修正（D 维度）**：KP15 的 `domainTotals` 从初始值更新为实际 quiz-engine 题量（javascript 18/css 6/react 9/network 9/security 6），并新增测试断言验证数据一致性。

6. **产品特性数据层就绪（B1/B2/B3/D1）**：
   - B1 自动评估：quiz-engine 题目已含 `tags`/`keyPoints`/`difficulty` 字段，数据层就绪（前端组件未实现自动评分 UI，记录为后续迭代项）
   - B2 代码编辑器：`codestepper` 组件可用于手写题步骤展示，但模拟面试中未集成 Monaco 编辑器（记录为后续迭代项）
   - B3 进度持久化：`progress-dashboard` 数据结构已定义，localStorage 持久化需前端组件层实现（记录为后续迭代项）
   - D1 标签筛选：quiz-engine 题目已含 `tags` 数组，前端筛选 UI 未实现（记录为后续迭代项）

7. **嵌套单引号修复**：CSS 动画性能题 detailedAnswer 中 `el.style.width = '100px'` 的内层单引号导致 oxc 解析器误判字符串终止，改为 `el.style.width = 100px`。

8. **types.test.ts 断言放宽**：`visualizationMeta` 键数量从精确匹配 105 改为 `>= 105`，兼容后续新增可视化类型。

#### 验证

```bash
npx tsc --noEmit                                    # exit 0
npx vitest run src/test/interview-prep.test.ts --reporter=dot
# Test Files  1 passed (1)
# Tests       30 passed (30)

npx vitest run                                       # 全套
# Test Files  27 passed (27)
# Tests       307 passed (307)
```

#### 关键决策记录

- **软技能/业务思维作为独立知识点**：KP16/KP17 作为 order 16/17 独立知识点，而非附在现有 KP 中。理由：高级别面试中软技能和业务理解是独立考察维度，权重与技术八股相当，独立知识点更便于用户定位和自测。
- **产品特性分层实现**：B1/B2/B3/D1 的数据层字段（tags/keyPoints/difficulty/domainTotals）已在模块数据中就绪，但前端交互组件（自动评分 UI/Monaco 编辑器/标签筛选 UI/localStorage 持久化）需后续迭代实现。理由：数据层变更影响面小且无破坏性，前端组件需独立设计评审，不宜在模块数据修复中一次性引入。
- **不补充职业发展内容**：用户明确选择不补充职业发展路径相关内容（晋升标准/薪资对标/成长案例），保持模块聚焦于面试八股与综合能力考察。
- **可视化资源分配策略**：CSS（KP2）使用 quiz-engine 而非 codestepper，因为 CSS 考点（Flex/Grid/选择器）更适合问答自测而非流程演示；浏览器（KP5）和安全（KP6）的原理性考点（渲染流程/攻击链）更适合 codestepper 步骤演示。

