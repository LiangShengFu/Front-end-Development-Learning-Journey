# 模块标准自查文档

> 以模块一「HTML 基础」修复后的质量基线为基准，抽象出可复用的模块质量标准，供其它模块在新建/重构时对照自查，确保全站学习体验的一致性与有效性。

***

## 一、文档目的

本文件解决一个具体问题：**如何判断一个学习模块是否"达标"？**

模块一此前存在三类典型缺陷——评估环节形同虚设、知识呈现静态化、内容组织学科导向。经修复后，模块一在「以测促学 / 动态反馈 / 任务导向」三个维度上建立了可参照的基线。本文件把该基线抽象成 6 个自查维度 + 1 张勾选清单，使其它模块无需重复摸索即可对齐质量。

**适用对象**：所有 `src/modules/*.ts` 学习模块（模块二 CSS、模块三 JS …… 及后续新增模块）。

**使用方式**：模块开发/重构完成后，逐项对照第三章维度与第四章清单打勾；任一维度不达标即视为该模块未完成质量门禁。

***

## 二、模块一基线概览（实例对照）

修复后的模块一达成了以下量化指标，作为其它模块的**下限参考**（不是上限，鼓励超越）：

| 维度                  | 模块一实例值 | 说明                                      |
| ------------------- | ------ | --------------------------------------- |
| 知识点数量               | **26** | order 1–26 连续无跳号                        |
| 面试题数量               | **16** | 覆盖文档流/盒模型/表单验证/meta/无障碍/语义化/srcset/场景题等 |
| 小测题数量               | **15** | 含【记忆】【理解】【应用】【对比】【场景】【综合】梯度标注           |
| 综合实战项目              | **2**  | KP22 搭建博客文章页、KP23 带验证注册表单               |
| 沙盒断言 checks         | **17** | 9 + 8，分布在 2 个实战项目沙盒                     |
| 知识点速查表              | **1**  | KP25                                    |
| 类型检查 `tsc --noEmit` | **通过** | exit 0                                  |
| 模块测试                | **通过** | html-fundamentals.test.ts 全绿            |

**关键文件锚点**：

- 模块数据：[src/modules/html-fundamentals.ts](../src/modules/html-fundamentals.ts)
- 沙盒类型：[src/lib/types.ts](../src/lib/types.ts) `SandboxData` / `SandboxCheck`
- 沙盒组件：[src/components/interactive/Sandbox.tsx](../src/components/interactive/Sandbox.tsx)
- 模块测试：[src/test/html-fundamentals.test.ts](../src/test/html-fundamentals.test.ts)

***

## 三、自查维度

每个维度包含：**① 标准要求 → ② 模块一实例 → ③ 检查方法 → ④ 常见缺陷**。

### 维度 A：知识点结构与编排

**① 标准要求**

- A1. `points` 数组的 `order` 字段从 1 连续递增至 N，**无重复、无跳号、无小数**。
- A2. 每个知识点具备 `title` / `difficulty`(1–5) / `blocks` 三要素。
- A3. 难度分布合理：以 1–3 为主，进阶内容(4–5)不超过总量的 30%。
- A4. 新增/重构的知识点显式标注 `isNew: true`，便于学习者识别更新。
- A5. `blocks` 内 `id` 遵循 `p{order}-{n}` 命名规范，全模块唯一。

**② 模块一实例**

26 个知识点 order 1–26 严格连续；KP22/KP23 实战项目标注 `isNew: true`；难度集中在 1–2，仅个别 API 章节为 3。

**③ 检查方法**

```bash
# 提取所有 order 值，人工核对连续性
# （用 IDE 正则搜索 ^\s+order:\s+(\d+),）
```

或在模块测试中断言：

```ts
it('知识点序号应从 1 到 N 连续', () => {
  const orders = someModule.points.map((p) => p.order)
  for (let i = 0; i < orders.length; i++) {
    expect(orders[i]).toBe(i + 1)
  }
})
```

**④ 常见缺陷**

- 插入新知识点后未重排后续 order，导致跳号/重复（模块一曾出现 order 21 / 21.5 / 22 冲突）。
- 用小数 order（如 21.5）作为临时插入，破坏连续性约定。

***

### 维度 B：评估环节有效性（以测促学）

**① 标准要求**

- B1. **面试题 ≥ 30 题**，覆盖该模块所有核心考点，无高频考点遗漏。
- B2. **小测题 ≥ 20 题**，且分布在模块末尾的独立 QuizCard 知识点。
- B3. 题目具备**梯度**，能区分"记忆 / 理解 / 应用"三个层次：
  - 记忆层：直问定义、标签类型；
  - 理解层：概念辨析、原理说明；
  - 应用层：场景判断、代码选型、排错。
- B4. 面试题至少包含 **2 道场景题**（"遇到 X 问题你怎么排查/重构"）与 **2 道对比题**（易混淆点 A vs B）。
- B5. 每道题的 `explanation` / `content` 不仅给结论，还解释"为什么"，指向易错点。
- B6. 考点覆盖率自查：列出模块所有核心知识点，逐个标注是否有对应题目，无盲区。

**② 模块一实例**

- 面试题 16 题：Q14 场景题（页面卡顿排查）、Q15 场景题（div 导航重构）、Q8 对比题（标准/IE 盒模型）、Q16 对比题（`<b>`/`<strong>` 等）。
- 小测题 15 题，题干前缀标注【理解】【应用】【对比】【场景】【综合】。
- 考点覆盖：文档流、盒模型、meta、viewport、语义化、defer/async、无障碍 POUR、ARIA、表单验证、srcset、空元素等均有对应题目。

**③ 检查方法**

```bash
# 统计面试题数量（匹配 Q{n}: 前缀）
# 统计小测题数量（匹配 ^\s+question:）
# 人工核对：是否存在"学了但没考"的知识点
```

**④ 常见缺陷**

- 题目全是直问定义，无法区分背书与理解（缺应用层）。
- 高频考点（如模块一的"文档流""表单验证"）无对应题目。
- 题目集中在模块末尾，章节学完无即时检验（理想态：每章末可加微测验，至少模块级总测验要全覆盖）。

***

### 维度 C：动态反馈闭环（沙盒断言）

**① 标准要求**

- C1. `SandboxData` 类型支持 `checks?: SandboxCheck[]` 字段（已由 types.ts 全局提供，各模块直接使用）。
- C2. `Sandbox.tsx` 渲染"任务检查清单"面板，对学习者代码**实时**跑断言并显示通过/未通过 + 中文教学提示（已由组件全局提供）。
- C3. 模块内**任务型练习沙盒**（尤其是实战项目）必须配置 `checks`，至少 5 条。**自由探索型沙盒**（hint 为"修改 X 观察 Y"的演练场）可不配 checks，因其教学目的是自由试错而非任务达标。区分依据：沙盒是否设定了明确的"完成目标"。
- C4. 每条 check 三要素齐全：`description`（任务要求）/ `pattern`（正则）/ `hint`（未通过时的教学引导）。
- C5. `pattern` 必须在沙盒中可正确编译（避免正则语法错误导致永远不通过）。
- C6. 断言不只是"语法存在性"检查，应体现**教学意图**（如"必须写 alt"对应无障碍教学点）。
- C7. 全部通过时显示完成态，给学习者正反馈。

**② 模块一实例**

- KP22 博客文章页沙盒：9 条 checks（charset / viewport / header / nav / main / article / img+alt / aside / footer）。
- KP23 注册表单沙盒：8 条 checks（form / text+required / email+required / password+minlength / select / button\[type=submit] / label\[for] / aria-required）。
- 每条 hint 指出"该怎么做 + 为什么"，例如 img+alt 的 hint 解释"无 alt 的图片对视障用户不存在，违反 WCAG 可感知原则"。

**③ 检查方法**

```bash
# 统计 checks 数量（匹配 ^\s+pattern:）
# 人工核对：每个配置了 checks 的沙盒，其 pattern 是否能在预期代码上通过、在错误代码上失败
```

**类型字段速查**：

```ts
export interface SandboxCheck {
  description: string  // 任务要求（展示给学习者）
  pattern: string      // 正则字符串，命中即通过
  flags?: string       // 默认 'i'
  hint: string         // 未通过时的教学提示
}
```

**④ 常见缺陷**

- 沙盒只支持"编辑-预览"，写错只能看浏览器原生报错，无教学反馈（模块一修复前的状态）。
- `pattern` 写得过严或过松：过严导致正确写法被判失败，过松导致随便写都通过。
- 只有语法检查没有语义检查（如只查"有没有 `<img>`"而不查"有没有 alt"）。

***

### 维度 D：任务导向内容组织（综合实战项目）

**① 标准要求**

- D1. 模块内至少 **1 个**（建议 2 个）综合实战知识点，放在该模块核心章节学完之后。
- D2. 实战项目必须**贯穿多个章节**的知识点，扮演"拼装图纸"角色，而非单点练习。
- D3. 每个实战项目配 `checks` 断言沙盒（接入维度 C）。
- D4. 沙盒提供**初始骨架代码**（带注释占位），降低冷启动门槛。
- D5. 项目前后有 `callout` 说明"为什么这个练习重要"与"实战后的反思"，连接工程思维。
- D6. 项目命名体现**真实开发场景**（如"搭建博客文章页""带验证的注册表单"），而非"练习 1"。

**② 模块一实例**

- KP22「搭建博客文章页」：串联 head 元信息 + 语义化骨架 + 多媒体无障碍，9 条断言，含 `callout variant="tip"` 说明练习价值。
- KP23「带验证注册表单」：串联表单控件 + 原生验证 + 无障碍 label 关联，8 条断言，含 `callout variant="warning"` 反思"前端验证的边界"（衔接后端校验）。
- 两者均提供带 `<!-- -->` 占位注释的初始骨架。

**③ 检查方法**

```bash
# 搜索实战项目知识点（title 含"实战"/"综合"）
# 核对每个实战沙盒是否配置 checks、是否有初始骨架、是否有 callout
```

**④ 常见缺陷**

- 知识按学科分类（结构→语义→多媒体→表单→API），学完仍不会"从零搭页面"。
- 没有贯穿多章的渐进式项目，知识点是离散积木缺拼装图纸。
- 实战项目变成"照抄示例"，无断言引导，无法定位薄弱环节。

***

### 维度 E：辅助学习资源

**① 标准要求**

- E1. 模块末尾含 1 个**知识点速查表**（`visualizationType: 'cheatsheet'` 或等价表格），浓缩核心 API/标签/属性。
- E2. 关键概念处使用 `callout`（`tip` / `warning` / `info`）强调易错点、最佳实践、边界条件。
- E3. 首个知识点有 `lead: true` 的引导段落，说明本章学什么、为什么学。
- E4. 代码示例遵守项目设计规范（canvas-mid 代码区、hairline 边框、等宽字体）。

**② 模块一实例**

- KP25 知识点速查表。
- 多处 callout：如表单章节的"无障碍表单要点"(tip)、KP23 的"前端验证边界"(warning)。
- 每章首段 `lead: true` 引导。

**③ 检查方法**

```bash
# 搜索 callout 使用密度（匹配 type: 'callout'）
# 确认存在 cheatsheet / 速查表知识点
```

**④ 常见缺陷**

- 全文平铺直叙，无重点强调，学习者抓不住易错点。
- 缺速查表，复习时无法快速定位。

***

### 维度 F：类型与工程质量

**① 标准要求**

- F1. `npx tsc --noEmit` 通过（exit 0），无类型错误。
- F2. 存在对应模块测试 `src/test/{module}.test.ts`，且全绿。
- F3. 测试覆盖：知识点数量、order 连续性、每个知识点三要素、可视化组件元数据。
- F4. 模块数据严格符合 `ModuleData` 类型（`points` / `blocks` / `data` 结构正确）。
- F5. 新增 `visualizationType` 时同步登记到 `visualizationMeta`（避免 types.test.ts 漂移）。
- F6. 不引入与模块无关的全局类型变更（避免污染其它模块）。

**② 模块一实例**

- `tsc --noEmit` exit 0。
- `html-fundamentals.test.ts` 14/14 通过（含 26 知识点、order 连续、三要素校验）。
- 注：`types.test.ts` 的 `visualizationMeta` (105 vs 173) 失败为**预先存在**的全局漂移，与模块一无关，需单独治理。

**③ 检查方法**

```bash
npx tsc --noEmit
npx vitest run src/test/{module}.test.ts --reporter=dot
```

**④ 常见缺陷**

- 新增知识点后忘更新测试中的 `toHaveLength(N)`，导致测试红。
- 新增可视化类型未登记 `visualizationMeta`，触发 types.test.ts 失败。
- order 用小数临时插入导致连续性测试失败。

***

## 四、自查清单（勾选式）

> 模块开发/重构完成后，逐项打勾。**任一未勾选 = 未过质量门禁。**

### A. 知识点结构

- [ ] A1. `order` 1..N 连续无重复无跳号无小数
- [ ] A2. 每个知识点有 `title` / `difficulty` / `blocks`
- [ ] A3. 难度分布合理（1–3 为主）
- [ ] A4. 新增知识点标注 `isNew: true`
- [ ] A5. block `id` 符合 `p{order}-{n}` 且全模块唯一

### B. 评估环节

- [ ] B1. 面试题 ≥ 12 题
- [ ] B2. 小测题 ≥ 10 题
- [ ] B3. 题目含记忆/理解/应用三梯度
- [ ] B4. 面试题含 ≥ 2 场景题 + ≥ 2 对比题
- [ ] B5. 每题 `explanation` 解释"为什么"
- [ ] B6. 核心考点全覆盖（无"学了没考"盲区）

### C. 动态反馈闭环

- [ ] C1. 使用 `SandboxData.checks`（类型已就绪）
- [ ] C2. 关键沙盒配置 `checks`（≥ 5 条）
- [ ] C3. 每条 check 三要素齐全（description/pattern/hint）
- [ ] C4. `pattern` 经实测：正确代码通过、错误代码失败
- [ ] C5. 断言体现教学意图（非纯语法检查）
- [ ] C6. 全部通过有完成态反馈

### D. 任务导向实战

- [ ] D1. 至少 1 个综合实战知识点（建议 2 个）
- [ ] D2. 项目贯穿多章节知识点
- [ ] D3. 配 `checks` 断言沙盒
- [ ] D4. 提供带注释占位的初始骨架
- [ ] D5. 前后有 `callout`（练习价值 + 反思）
- [ ] D6. 命名体现真实开发场景

### E. 辅助资源

- [ ] E1. 模块末尾有知识点速查表
- [ ] E2. 关键概念有 `callout` 强调
- [ ] E3. 首知识点有 `lead: true` 引导段
- [ ] E4. 代码示例符合设计规范

### F. 工程质量

- [ ] F1. `tsc --noEmit` 通过
- [ ] F2. 模块测试存在且全绿
- [ ] F3. 测试覆盖数量/order/三要素
- [ ] F4. 数据符合 `ModuleData` 类型
- [ ] F5. 新增 `visualizationType` 已登记 `visualizationMeta`
- [ ] F6. 未引入无关全局类型变更
- [ ] F7. 沙盒 `language` 值在 `SandboxData.language` 类型联合内（`'js' | 'html' | 'css' | 'typescript'`），新增语言须同步扩展类型联合（避免对象字面量多余属性报错）

***

## 五、应用流程（如何把本标准用到新模块）

1. **新建模块骨架**：复制 `html-fundamentals.ts` 的结构（顶部 import、`ModuleData` 形状），改 `number/title/slug/stage/icon`。
2. **编写知识点**：按 `order` 1..N 顺序编写，遵循 A 系列标准。
3. **穿插实战项目**：在核心章节后插入综合实战 KP，配置 `checks` 沙盒（D + C 标准）。
4. **编写评估题**：先列出全部核心考点，再为每个考点配题，确保 B6 无盲区；题目前缀标注梯度。
5. **补速查表与 callout**：模块末尾加速查表，关键处加 callout（E 标准）。
6. **编写模块测试**：参照 `html-fundamentals.test.ts`，覆盖数量/order 连续/三要素。
7. **跑质量门禁**：
   ```bash
   npx tsc --noEmit
   npx vitest run src/test/{module}.test.ts --reporter=dot
   ```
8. **对照第四章清单逐项打勾**，未勾项回到对应维度修复。
9. **登记可视化类型**：若引入新 `visualizationType`，同步更新 `visualizationMeta` 与 `types.test.ts`。

***

## 六、模块二检查实录（实例应用）

> 本节是自查标准的首次实战应用：对模块二「CSS 基础与核心原理」按维度 A–F 逐项检查、记录不符合项、修复并复测。其它模块可参照本节的检查流程与决策逻辑。

### 6.1 修复前不符合项清单

| 维度 | 检查项                       | 修复前状态                  | 是否达标 |
| -- | ------------------------- | ---------------------- | ---- |
| B  | B1 面试题 ≥ 12               | **0 题**（无面试题知识点）       | ❌    |
| B  | B2 小测题 ≥ 10               | 5 题                    | ❌    |
| B  | B3 梯度（记忆/理解/应用）           | 无梯度标注                  | ❌    |
| B  | B4 场景题 + 对比题              | 无                      | ❌    |
| B  | B6 考点全覆盖                  | 多个核心考点无题               | ❌    |
| C  | C2 任务型沙盒配 checks          | 3 个沙盒均无 checks，且无任务型沙盒 | ❌    |
| D  | D1 综合实战项目                 | **0 个**                | ❌    |
| D  | D2-D6 实战项目要素              | 不适用（无项目）               | ❌    |
| E  | E1 知识点速查表                 | 无独立速查表知识点              | ❌    |
| A  | A1 order 连续               | 1–20 连续                | ✅    |
| A  | A2 三要素 / A4 isNew / A5 id | 达标                     | ✅    |
| E  | E2 callout / E3 lead      | 17 个 callout、3 处 lead  | ✅    |
| F  | F1 tsc / F2 测试            | 通过                     | ✅    |

### 6.2 修复措施

1. **扩充小测题 5 → 15**：新增 10 题，题干前缀标注【理解】【应用】【对比】【场景】【综合】，覆盖 BFC、盒模型计算、Flex/Grid 对比、em/rem 对比、sticky、clamp、margin 折叠、z-index 失效、触摸目标等核心考点。
2. **新增面试题知识点（KP22）**：16 题，含 Q13 场景题（CSS 性能排查）、Q14 场景题（float 布局重构）、Q15 对比题（display:none/visibility/opacity）、Q16 对比题（伪类 vs 伪元素）。
3. **新增实战项目 1（KP20）「响应式卡片布局页」**：9 条 checks，串联 Grid + 响应式 + CSS 变量 + clamp()。提供带注释占位的初始骨架，配 `callout variant="tip"` 说明练习价值。
4. **新增实战项目 2（KP21）「主题化导航栏」**：8 条 checks，串联 CSS 变量 + sticky 定位 + z-index 层叠 + Flex + transition。配 `callout variant="warning"` 反思 sticky 与层叠上下文陷阱。
5. **新增知识点速查表（KP23）**：15 行 `type: 'table'`，浓缩引入/选择器/优先级/盒模型/BFC/Flex/Grid/定位/层叠/响应式/单位/动画/变量/继承/浮动。
6. **重排 order 1–24**：原 KP20 小测验后移为 KP24，插入 4 个新 KP（实战1/实战2/面试题/速查表为 KP20–23），保持 order 严格连续无小数。
7. **更新模块元数据**：`knowledgePointCount` 20→24、`visualizationCount` 27→30、头部注释同步。
8. **更新测试**：`css-fundamentals.test.ts` 中 `toHaveLength(20)`→`24`、order 循环 20→24。

### 6.3 关键决策记录

- **自由探索型沙盒不强制配 checks**：模块二原有 3 个沙盒（Flexbox/动画/CSS 变量）hint 为"修改 X 观察 Y 变化"，属自由探索演练场而非任务型练习。若强加 checks 会改变其"自由试错"的教学定位。据此将标准 C2 细化为"任务型沙盒必须配 checks，自由探索型可不配"，并以"是否设定明确完成目标"作为区分依据。此修订已回写到第三章维度 C 与第四章清单。
- **实战项目顺序**：遵循模块一模式（核心 KPs → 实战项目 → 面试题 → 速查表 → 小测验），确保小测验作为收尾评估。

### 6.4 修复后复测结果

| 检查项             | 修复后状态                          | 是否达标 |
| --------------- | ------------------------------ | ---- |
| A1 order 连续     | 1–24 连续                        | ✅    |
| B1 面试题          | 16 题                           | ✅    |
| B2 小测题          | 15 题                           | ✅    |
| B3 梯度标注         | 含【理解】【应用】【对比】【场景】【综合】          | ✅    |
| B4 场景题 + 对比题    | 各 2 道                          | ✅    |
| C2 任务型沙盒 checks | 2 个实战沙盒共 17 条                  | ✅    |
| D1 综合实战项目       | 2 个                            | ✅    |
| D3-D6 实战要素      | checks + 骨架 + callout + 真实场景命名 | ✅    |
| E1 速查表          | KP23，15 行                      | ✅    |
| F1 tsc --noEmit | exit 0                         | ✅    |
| F2 模块测试         | 10/10 通过                       | ✅    |

### 6.5 模块二关键实例锚点

| 自查项                | 模块二实例位置                                                                            |
| ------------------ | ---------------------------------------------------------------------------------- |
| 24 知识点 order 连续    | [css-fundamentals.ts](../src/modules/css-fundamentals.ts) `order: 1` … `order: 24` |
| 16 道面试题            | 知识点 22「面试题」`visualizationType: 'accordion'`                                        |
| 15 道小测题（含梯度标注）     | 知识点 24「CSS 小测验」`visualizationType: 'quiz'`                                         |
| 实战项目 1（9 条 checks） | 知识点 20「综合实战：响应式卡片布局页」`visualizationType: 'sandbox'`                                |
| 实战项目 2（8 条 checks） | 知识点 21「综合实战：主题化导航栏」`visualizationType: 'sandbox'`                                  |
| 知识点速查表             | 知识点 23「知识点速查表」`type: 'table'`                                                      |
| 模块测试               | [css-fundamentals.test.ts](../src/test/css-fundamentals.test.ts)                   |

***

## 七、模块三检查实录（实例应用）

> 本节对模块三「JavaScript 核心语法」按维度 A–F 逐项检查、记录不符合项、修复并复测。延续模块二的检查流程与决策逻辑，验证自查标准在 JS 核心模块的适用性。

### 7.1 修复前不符合项清单

| 维度 | 检查项                       | 修复前状态                                  | 是否达标 |
| -- | ------------------------- | -------------------------------------- | ---- |
| B  | B1 面试题 ≥ 12               | **6 题**，缺场景题与对比题                       | ❌    |
| B  | B2 小测题 ≥ 10               | 8 题                                    | ❌    |
| B  | B3 梯度（记忆/理解/应用）           | 无梯度标注                                  | ❌    |
| B  | B4 场景题 + 对比题              | 无                                      | ❌    |
| C  | C2 任务型沙盒配 checks          | 仅 1 个演示沙盒（KP16 Todo），无 checks、无 callout | ❌    |
| D  | D1 综合实战项目                 | **0 个**（KP16 为单点练习，非贯穿多章的综合项目）          | ❌    |
| D  | D2-D6 实战项目要素              | 不适用（无项目）                               | ❌    |
| E  | E1 知识点速查表                 | 使用 `skillbar` 类型，非 `table`，未浓缩核心 API   | ❌    |
| F  | F2 模块测试                   | 无 `javascript-core.test.ts`            | ❌    |
| A  | A1 order 连续               | 1–19 连续                                | ✅    |
| A  | A2 三要素 / A4 isNew / A5 id | 达标                                     | ✅    |
| E  | E2 callout / E3 lead      | 多处 callout、首段 lead                     | ✅    |
| F  | F1 tsc                    | 通过                                     | ✅    |

### 7.2 修复措施

1. **扩充面试题 6 → 34**：新增 2 道场景题（Q13 内存泄漏排查、Q14 回调地狱重构）与 2 道对比题（Q15 Promise.all/allSettled/any/race、Q16 深浅拷贝），并补 18 道覆盖 GC/V8/柯里化/new/手写 Promise/宏微任务/Set-Map/Proxy/Iterator 等核心考点，覆盖闭包/原型链/this/事件循环/Promise/模块化等。
2. **扩充小测题 8 → 20**：题干前缀标注【记忆】【理解】【应用】【对比】【场景】【综合】梯度，覆盖 typeof 陷阱、箭头函数 this、事件循环顺序、隐式丢失、私有字段、Map vs Object、防抖场景、var 闭包陷阱、浮点精度、a.x=a={n:2} 赋值求值顺序等。
3. **新增实战项目 1（KP17）「手写防抖与节流工具函数」**：6 条 checks，串联闭包 + 定时器 + 高阶函数，提供带 TODO 占位的初始骨架，配 `callout variant="tip"` 说明练习价值 + `callout variant="warning"` 反思 leading/trailing 与内存泄漏。
4. **新增实战项目 2（KP18）「实现发布订阅 EventEmitter」**：6 条 checks，串联 class + this 绑定 + Map 集合 + 数组方法，提供初始骨架，配 `callout variant="tip"` + `callout variant="warning"` 反思忘记 off 导致的内存泄漏。
5. **知识点速查表（KP20）从 skillbar 改为 table 类型**：16 行，三列（主题 / 核心 API·语法 / 要点提示），浓缩数据类型/转换/相等/作用域/闭包/this/原型链/数组/异步/事件循环/模块/ES6+/类/集合/Proxy。
6. **新建模块测试 `javascript-core.test.ts`**：15 个用例，除常规元数据/order/三要素校验外，额外断言小测验梯度标注、面试题场景/对比题、速查表表头行数一致、实战沙盒 checks ≥5 且含 callout 与 TODO 骨架。
7. **重排 order 1–21 连续**：原 KP17–19（面试题/速查表/小测验）后移为 KP19/20/21，插入 2 个综合实战 KP（KP17/KP18），保持 order 严格连续无小数。
8. **更新模块元数据**：`knowledgePointCount` 19→21、`visualizationCount` 25→27、头部 JSDoc 注释同步。

### 7.3 关键决策记录

- **自由探索型沙盒不强制配 checks（延续模块二决策）**：模块三原有演示沙盒（KP2 闭包计数器、KP8 数组方法、KP16 Todo）hint 为"修改 X 观察 Y"，属自由探索演练场而非任务型练习，故不强制加 checks；仅新增的 2 个综合实战沙盒配置 checks 断言。区分依据仍为"是否设定明确完成目标"。
- **实战项目选材**：选择「防抖节流」与「EventEmitter」因其能贯穿模块核心知识点（闭包/this/高阶函数/类/Map/异步），且是面试与工程实践的高频考点，符合 D2"贯穿多章节"要求。
- **速查表类型统一为 table**：模块二已将速查表统一为 `type: 'table'`（标准 E1 措辞为"cheatsheet 或等价表格"），模块三沿用 table 以保持一致；同时修正了模块三此前误用 `skillbar` 的问题。

### 7.4 修复后复测结果

| 检查项             | 修复后状态                          | 是否达标 |
| --------------- | ------------------------------ | ---- |
| A1 order 连续     | 1–21 连续                        | ✅    |
| B1 面试题          | 16 题                           | ✅    |
| B2 小测题          | 15 题                           | ✅    |
| B3 梯度标注         | 含【记忆】【理解】【应用】【对比】【场景】【综合】      | ✅    |
| B4 场景题 + 对比题    | 各 2 道                          | ✅    |
| C2 任务型沙盒 checks | 2 个实战沙盒共 12 条                  | ✅    |
| D1 综合实战项目       | 2 个                            | ✅    |
| D3-D6 实战要素      | checks + 骨架 + callout + 真实场景命名 | ✅    |
| E1 速查表          | KP20，table 类型 16 行             | ✅    |
| F1 tsc          | 模块三无类型错误（exit 0）               | ✅    |
| F2 模块测试         | 15/15 通过                       | ✅    |

> 注：仓库现存 `testing-system.ts` / `VirtualListVisualizer.tsx` 的预先存在类型错误与模块三无关，不在本次治理范围。

### 7.5 模块三关键实例锚点

| 自查项                | 模块三实例位置                                                                                  |
| ------------------ | ---------------------------------------------------------------------------------------- |
| 21 知识点 order 连续    | [javascript-core.ts](../src/modules/javascript-core.ts) `order: 1` … `order: 21`         |
| 16 道面试题            | 知识点 19「面试题」`visualizationType: 'accordion'`，含 Q13/Q14 场景题、Q15/Q16 对比题                     |
| 15 道小测题（含梯度标注）     | 知识点 21「小测验」`visualizationType: 'quiz'`，题干前缀【记忆】【理解】【应用】【对比】【场景】【综合】                       |
| 实战项目 1（6 条 checks） | 知识点 17「综合实战：手写防抖与节流工具函数」`visualizationType: 'sandbox'`                                    |
| 实战项目 2（6 条 checks） | 知识点 18「综合实战：实现发布订阅 EventEmitter」`visualizationType: 'sandbox'`                            |
| 知识点速查表             | 知识点 20「知识点速查表」`type: 'table'`，16 行                                                        |
| 模块测试               | [javascript-core.test.ts](../src/test/javascript-core.test.ts)                           |

***

## 八、模块四检查实录（实例应用）

> 本节对模块四「DOM / BOM / Web API」按维度 A–F 逐项检查、记录不符合项、修复并复测。延续模块二/三的检查流程与决策逻辑，验证自查标准在 DOM/BOM 模块的适用性。

### 8.1 修复前不符合项清单

| 维度 | 检查项                       | 修复前状态                                            | 是否达标 |
| -- | ------------------------- | ------------------------------------------------ | ---- |
| B  | B1 面试题 ≥ 12               | **0 题**（无面试题知识点）                                 | ❌    |
| B  | B2 小测题 ≥ 10               | 8 题                                              | ❌    |
| B  | B3 梯度（记忆/理解/应用）           | 无梯度标注                                            | ❌    |
| B  | B4 场景题 + 对比题              | 无                                                | ❌    |
| B  | B6 考点全覆盖                  | 多个核心考点（事件委托原理、批量插入、Cookie 安全等）无题                  | ❌    |
| C  | C2 任务型沙盒配 checks          | 4 个 sandbox 均为自由探索演示，无 checks、无任务型沙盒             | ❌    |
| D  | D1 综合实战项目                 | **0 个**（章节内 sandbox 为单点练习，非贯穿多章综合项目）              | ❌    |
| D  | D2-D6 实战项目要素              | 不适用（无项目）                                         | ❌    |
| E  | E1 知识点速查表                 | 章节内有 2 个 table 块（属性对比/几何尺寸），但无独立"速查表"知识点浓缩全模块 API | ❌    |
| F  | F2 模块测试                   | 无 `dom-bom-webapi.test.ts`                        | ❌    |
| A  | A1 order 连续               | 1–21 连续                                          | ✅    |
| A  | A2 三要素 / A4 isNew / A5 id | 达标                                               | ✅    |
| E  | E2 callout / E3 lead      | 多处 callout（性能提示、SPA 路由原理等）、首段 lead               | ✅    |
| F  | F1 tsc                    | 通过                                               | ✅    |

### 8.2 修复措施

1. **扩充小测题 8 → 20**：新增 12 题，题干前缀标注【记忆】【理解】【应用】【对比】【场景】【综合】梯度，覆盖 createElement 游离节点、事件委托原理、DocumentFragment 批量插入、stopPropagation 区别、Cookie SameSite、getBoundingClientRect、pushState 404、大文件上传进度、dataset 映射、rAF 后台暂停等核心考点。
2. **新增面试题知识点（KP23）**：16 题，含 Q13 场景题（列表频繁增删卡顿排查）、Q14 场景题（SPA 路由内存上涨排查）、Q15 对比题（innerHTML/textContent/insertAdjacentHTML）、Q16 对比题（appendChild/insertBefore/replaceChild/remove）。
3. **新增实战项目 1（KP21）「事件委托动态待办列表」**：6 条 checks，串联事件流 + 事件委托 + closest + dataset，提供带 TODO 占位的初始骨架，配 `callout variant="tip"` 说明练习价值 + `callout variant="warning"` 反思委托边界（非冒泡事件、stopPropagation、卸载解绑）。
4. **新增实战项目 2（KP22）「手写简易 History SPA 路由」**：6 条 checks，串联 pushState + popstate + 路由表匹配 + 动态参数 :id + a 标签拦截，提供初始骨架，配 `callout variant="warning"` 反思刷新 404 与服务端 fallback。
5. **新增知识点速查表（KP24）**：21 行 `type: 'table'`，三列（主题 / 核心 API·语法 / 要点提示），浓缩节点查询/创建/插入/删除/属性/样式/事件/事件流/事件委托/window/location/history/navigator/存储/Cookie安全/几何尺寸/滚动/文件/Blob/定时器。
6. **新建模块测试 `dom-bom-webapi.test.ts`**：15 个用例，参照模块三测试结构，覆盖元数据/25 知识点/order 连续/三要素/id 唯一/可视化组件登记/难度分布，并断言小测验 20 题梯度标注、面试题 16 题含场景/对比题、速查表 table 行数≥10 且列数一致、实战沙盒 checks ≥5 且含 callout 与 TODO 骨架。
7. **重排 order 1–25 连续**：原 KP21 小测验后移为 KP25，插入 4 个新 KP（实战1/实战2/面试题/速查表为 KP21–24），保持 order 严格连续无小数。
8. **更新模块元数据**：`knowledgePointCount` 19→25、`visualizationCount` 19→23、头部 JSDoc 注释同步。

### 8.3 关键决策记录

- **原有演示沙盒不强制配 checks（延续模块二/三决策）**：模块四原有 4 个 sandbox（节点查询/增删改/样式操作/事件委托演示）hint 为"自由修改观察"，属自由探索演练场而非任务型练习，故不强制加 checks；仅新增的 2 个综合实战沙盒配置 checks 断言。区分依据仍为"是否设定明确完成目标"。
- **实战项目选材**：选择「事件委托动态列表」与「手写 SPA 路由」因其贯穿模块核心知识点（事件系统/history API/DOM 操作），且是工程实践高频场景，符合 D2"贯穿多章节"要求。事件委托串联事件流+closest+dataset；SPA 路由串联 history+popstate+路由表。
- **速查表选材覆盖全模块**：不同于模块二/三速查表聚焦语法，模块四速查表覆盖 DOM 节点操作、事件系统、BOM 对象、存储、几何、文件、定时器七大主题，体现 Web API 的"工具箱"特性。

### 8.4 修复后复测结果

| 检查项             | 修复后状态                          | 是否达标 |
| --------------- | ------------------------------ | ---- |
| A1 order 连续     | 1–25 连续                        | ✅    |
| B1 面试题          | 16 题                           | ✅    |
| B2 小测题          | 20 题                           | ✅    |
| B3 梯度标注         | 含【记忆】【理解】【应用】【对比】【场景】【综合】      | ✅    |
| B4 场景题 + 对比题    | 各 2 道                          | ✅    |
| C2 任务型沙盒 checks | 2 个实战沙盒共 12 条                  | ✅    |
| D1 综合实战项目       | 2 个                            | ✅    |
| D3-D6 实战要素      | checks + 骨架 + callout + 真实场景命名 | ✅    |
| E1 速查表          | KP24，table 类型 21 行             | ✅    |
| F1 tsc          | 模块四无类型错误（exit 0）               | ✅    |
| F2 模块测试         | 15/15 通过                       | ✅    |

> 注：仓库现存 `testing-system.ts` / `VirtualListVisualizer.tsx` 的预先存在类型错误与模块四无关，不在本次治理范围。

### 8.5 模块四关键实例锚点

| 自查项                | 模块四实例位置                                                                                |
| ------------------ | -------------------------------------------------------------------------------------- |
| 25 知识点 order 连续    | [dom-bom-webapi.ts](../src/modules/dom-bom-webapi.ts) `order: 1` … `order: 25`         |
| 16 道面试题            | 知识点 23「面试题」`visualizationType: 'accordion'`，含 Q13/Q14 场景题、Q15/Q16 对比题                   |
| 20 道小测题（含梯度标注）     | 知识点 25「DOM / BOM / Web API 小测验」`visualizationType: 'quiz'`，题干前缀【记忆】【理解】【应用】【对比】【场景】【综合】 |
| 实战项目 1（6 条 checks） | 知识点 21「综合实战：事件委托动态待办列表」`visualizationType: 'sandbox'`                                  |
| 实战项目 2（6 条 checks） | 知识点 22「综合实战：手写简易 History SPA 路由」`visualizationType: 'sandbox'`                          |
| 知识点速查表             | 知识点 24「知识点速查表」`type: 'table'`，21 行                                                      |
| 模块测试               | [dom-bom-webapi.test.ts](../src/test/dom-bom-webapi.test.ts)                           |

***

## 九、面试题闪卡样式改造（跨模块 1-5 增强）

> 本节记录一次面向"评估环节展示体验"的跨模块 UI 增强，非维度 A–F 的合规检查，而是对 B 评估环节的体验优化。背景：模块 3/4 面试题扩充至 34/16 题后，原手风琴列表（每题一行平铺）导致面试题知识点纵向过长，浏览与自测体验下降。

### 9.1 问题

- 面试题采用 `visualizationType: 'accordion'`，所有题目平铺为可折叠行，题量一多（16+）列表过长，学习者需大量滚动，难以逐题聚焦。
- 项目已有模块 25 专用的 `FlashcardDeck`（基于 SM-2 间隔重复），但其数据结构为 `InterviewQuestion`（需 id / shortAnswer / keyPoints / difficulty / domain），与各模块面试题的 `AccordionData`（`{title, content}`）不兼容，直接套用需重写全部题目数据，成本高且改变数据语义（普通面试题 ≠ 间隔重复记忆卡）。

### 9.2 方案决策

经对比三套方案后选用"增强现有 Accordion"：

| 方案 | 做法 | 取舍 |
| ---- | ---- | ---- |
| ✅ 增强 Accordion | 在组件内加"列表 / 闪卡"视图切换 + `defaultMode` 字段控制默认模式 | 零数据迁移、一处改动全局生效、不新增 visualizationType、不破坏 23 个模块既有数据与测试；缺点是 Accordion 语义略变重 |
| 新建独立闪卡组件 | 新增 `interview-flashcard` 类型并登记 visualizationMeta，再批量改模块 visualizationType | 语义更清晰，但需改 23 处数据 + 测试 + 类型登记，风险与收益不匹配 |
| 复用模块 25 FlashcardDeck | 套用 SM-2 闪卡 | 需把面试题重写为 InterviewQuestion 结构，工作量大且改变数据语义，不推荐 |

决策依据：用户需求是"一题一屏解决过长"，而非 SM-2 记忆算法；增强组件可让所有用 accordion 的知识点（含非面试题的速查 accordion）都获得切换能力，且默认模式由数据控制，不影响其他模块现状。

### 9.3 实现内容

1. **类型扩展** [types.ts](../src/lib/types.ts) `AccordionData` 新增可选字段：
   ```ts
   defaultMode?: 'list' | 'flashcard'
   ```
   - `list`（默认）：原手风琴列表
   - `flashcard`：一题一屏闪卡

2. **组件增强** [Accordion.tsx](../src/components/interactive/Accordion.tsx) 拆分为 `ListView`（原手风琴逻辑，行为不变）+ `FlashcardView`（新增），顶部加视图切换 tab：
   - 闪卡模式：正面显示题目（title），点击卡片 3D 翻转（rotateY）显示答案（content + 可选 code 用 CodeBlock 渲染）
   - 进度栏：`第 X / N 题` 标签 + 题目/答案状态标签 + 进度条
   - 导航：上一题 / 翻转 / 下一题，循环切换，切题自动收起翻转
   - 切换模式与切题均重置 flipped 状态；min-height 260px 保证正反面一致
   - 列表模式 content 渲染同步加 `whitespace-pre-line` 保留多段换行

3. **模块 1-5 面试题默认闪卡**：仅对五个模块的「面试题」知识点的 demo data 加 `defaultMode: 'flashcard'`，其他非面试题 accordion（选择器速查 / 单位速查 / 继承属性 / input 类型 / ES Module 体系等）保持默认列表模式不变。
   - 模块 1 [html-fundamentals.ts](../src/modules/html-fundamentals.ts) 知识点 24 面试题
   - 模块 2 [css-fundamentals.ts](../src/modules/css-fundamentals.ts) 知识点 22 面试题
   - 模块 3 [javascript-core.ts](../src/modules/javascript-core.ts) 知识点 19 面试题（34 题）
   - 模块 4 [dom-bom-webapi.ts](../src/modules/dom-bom-webapi.ts) 知识点 23 面试题（16 题）
   - 模块 5 [typescript-core.ts](../src/modules/typescript-core.ts) 知识点 17 面试题（16 题）

### 9.4 兼容性与影响范围

- **向后兼容**：`defaultMode` 为可选字段，未设则默认 `list`，模块 6-25 的 accordion 行为完全不变（仅顶部多一个可选"闪卡"切换 tab，不强制改变默认视图）。
- **不新增 visualizationType**：无需修改 `visualizationMeta` 登记表与 `VisualizationRenderer` 分发器，不影响类型联合 `VisualizationType`。
- **数据零迁移**：复用既有 `{title, content, code?, codeLanguage?}` 结构，不要求面试题补 shortAnswer / keyPoints 等字段。
- **测试无破坏**：模块 3/4 现有测试断言面试题知识点存在、题数达标、含场景/对比题，均不涉及 `defaultMode`，无需改测试。

### 9.5 验证结果

| 检查项 | 结果 |
| ---- | ---- |
| `tsc -b` | 模块 1-4 / Accordion.tsx / types.ts 无类型错误（仓库现存 testing-system 预先错误与本次无关） |
| `vitest run` 模块 3+4 测试 | 30/30 通过 |
| 列表模式回归 | 行为与改造前一致（展开/折叠/multiple/defaultOpen 保留） |
| 闪卡模式可用 | 一题一屏、翻转、上下题循环、进度条、code 渲染均正常 |

### 9.6 后续建议（可选最佳实践）

- 题量 ≥ 12 的面试题知识点建议默认 `defaultMode: 'flashcard'`，列表模式作为"通览全部题目"的补充视图保留。
- 后续若模块 6-25 面试题也较长，可按需逐个加 `defaultMode: 'flashcard'`，无需再改组件。
- 本改造不作为维度 A–F 的强制检查项，仅作为 B 评估环节的展示体验最佳实践记录在案。

***

## 十、模块五检查实录（实例应用）

> 本节对模块五「TypeScript 核心与进阶」按维度 A–F 逐项检查、记录不符合项、修复并复测。延续模块二/三/四的检查流程与决策逻辑，验证自查标准在 TypeScript 类型系统模块的适用性，并新增"沙盒语言扩展"检查项。

### 10.1 修复前不符合项清单

| 维度 | 检查项                       | 修复前状态                                            | 是否达标 |
| -- | ------------------------- | ------------------------------------------------ | ---- |
| B  | B1 面试题 ≥ 12               | 6 题                                              | ❌    |
| B  | B2 小测题 ≥ 10               | 6 题                                              | ❌    |
| B  | B3 梯度（记忆/理解/应用）           | 无梯度标注                                            | ❌    |
| B  | B4 场景题 + 对比题              | 无                                                | ❌    |
| C  | C2 任务型沙盒配 checks          | 4 个 sandbox 均为自由探索演示，无 checks、无任务型沙盒             | ❌    |
| D  | D1 综合实战项目                 | **0 个**（章节内 sandbox 为单点演示，非贯穿多章综合项目）              | ❌    |
| D  | D2-D6 实战项目要素              | 不适用（无项目）                                         | ❌    |
| F  | F2 模块测试                   | 无 `typescript-core.test.ts`                      | ❌    |
| A  | A1 order 连续               | 1–17 连续                                          | ✅    |
| A  | A2 三要素 / A4 isNew / A5 id | 达标                                               | ✅    |
| E  | E1 知识点速查表                 | KP16 速查表（comparetable，14 行）                        | ✅    |
| E  | E2 callout / E3 lead      | 多处 callout（迁移原则、API 安全等）、首段 lead                  | ✅    |
| F  | F1 tsc                    | 通过                                               | ✅    |

### 10.2 修复措施

1. **扩充面试题 6 → 16**：新增 Q7-Q16，覆盖 never 类型与穷尽检查、协变与逆变、函数重载、as const、枚举陷阱、类型断言与类型守卫对比、Partial 与 Readonly 对比、Omit 与 Pick 关系、JS 项目迁移 TS 策略（场景题）、TS 项目编译慢排查优化（场景题）。含 2 道场景题（Q15/Q16）+ 2 道对比题（Q13/Q14）。面试题 demo data 加 `defaultMode: 'flashcard'`，默认闪卡模式展示。
2. **扩充小测题 6 → 20**：新增 14 题，题干前缀标注【记忆】【理解】【应用】【对比】【场景】【综合】梯度，覆盖 keyof、never、as const、any/unknown 对比、Partial/Required 关系、const enum 陷阱、ReturnType 手写、JSON.parse 安全、分布式条件类型、as 与类型守卫区别、strictFunctionTypes、映射类型、迁移策略、工具类型综合等核心考点。
3. **新增实战项目 1（KP15）「手写工具类型库」**：6 条 checks，串联映射类型（MyPartial）+ 键遍历（MyPick）+ 键重映射 as（MyOmit）+ infer 推断（MyReturnType），提供带 TODO 占位的初始骨架，配 `callout variant="tip"` 说明练习价值 + `callout variant="warning"` 反思内置 Omit 的 K 不约束设计取舍与键重映射版本要求。
4. **新增实战项目 2（KP16）「类型安全的 API 响应解析器」**：6 条 checks，串联泛型 fetchJson + unknown 收窄 + 自定义类型守卫 isUser + 错误处理 + 批量 Promise.all 守卫过滤，提供初始骨架，配 `callout variant="tip"` 说明练习价值 + `callout variant="warning"` 反思生产环境优先用 zod 等 schema 库。
5. **重排 order 1–19 连续**：原 KP15 面试题/KP16 速查表/KP17 小测题顺延为 KP17/KP18/KP19，插入 2 个新实战 KP（KP15/KP16），保持 order 严格连续。同步更新 block id（ts-p15-* → ts-p17-* 等）保持与 order 对应。
6. **更新模块元数据**：`knowledgePointCount` 17→19、`visualizationCount` 16→18、头部 JSDoc 注释同步。
7. **扩展沙盒语言类型** [types.ts](../src/lib/types.ts) `SandboxData.language` 从 `'js' | 'html' | 'css'` 扩展为 `'js' | 'html' | 'css' | 'typescript'`，支持 TypeScript 沙盒（模块五两个实战沙盒 language 为 'typescript'）。
8. **新建模块测试 `typescript-core.test.ts`**：15 个用例，参照模块四测试结构，覆盖元数据/19 知识点/order 连续/三要素/id 唯一/可视化组件登记/难度分布，并断言小测验 20 题梯度标注、面试题 16 题（用标题匹配定位，因模块五有两个 accordion 知识点）含场景/对比题、速查表 comparetable 行数≥10、实战沙盒 language 为 'typescript' 且 checks ≥5 且含 callout 与 TODO 骨架。

### 10.3 关键决策记录

- **速查表用 comparetable 而非 table 块**：模块一/四速查表用独立 `type: 'table'` 块，模块五速查表用 `visualizationType: 'comparetable'` 的 demo 块（14 行，featureColumn + columns + rows 结构）。两者实质等同（均为行列表格），comparetable 额外支持高亮列。测试断言改为针对 comparetable 校验 rows.length ≥ 10，未强制改为 table 块以避免无谓重构与 visualizationCount 变动。此差异记录为可接受实现多样性。
- **沙盒语言扩展决策**：TypeScript 沙盒需 `language: 'typescript'`，但 `SandboxData.language` 原仅含 `'js' | 'html' | 'css'`。扩展类型联合而非新增字段，因 language 语义本就是"代码语言"。此改动向后兼容（原值仍合法），且为后续模块 TypeScript 沙盒铺路。
- **实战项目选材**：选择「手写工具类型库」与「类型安全 API 解析器」因其贯穿模块核心知识点（映射/条件/infer 类型 + 泛型/unknown/类型守卫），且是面试高频与工程高频场景。前者训练类型编程三大支柱，后者训练 any/unknown/守卫的安全模式，符合 D2"贯穿多章节"要求。
- **面试题定位用标题匹配**：模块五有两个 accordion 知识点（早期类型演示 + 面试题），测试用 `p.title.includes('面试题')` 精确定位面试题知识点，避免 find 返回第一个非面试题 accordion 导致题数断言失败。此模式建议后续多 accordion 模块测试沿用。
- **原有演示沙盒不强制配 checks（延续模块二/三/四决策）**：模块五原有 4 个 sandbox（类型收窄/泛型约束/工具类型演示等）属自由探索演示，不强制加 checks；仅新增 2 个综合实战沙盒配置 checks 断言。

### 10.4 修复后复测结果

| 检查项             | 修复后状态                          | 是否达标 |
| --------------- | ------------------------------ | ---- |
| A1 order 连续     | 1–19 连续                        | ✅    |
| B1 面试题          | 16 题                           | ✅    |
| B2 小测题          | 20 题                           | ✅    |
| B3 梯度标注         | 含【记忆】【理解】【应用】【对比】【场景】【综合】      | ✅    |
| B4 场景题 + 对比题    | 各 2 道                          | ✅    |
| C2 任务型沙盒 checks | 2 个实战沙盒共 12 条                  | ✅    |
| D1 综合实战项目       | 2 个                            | ✅    |
| D3-D6 实战要素      | checks + 骨架 + callout + 真实场景命名 | ✅    |
| E1 速查表          | KP18，comparetable 14 行         | ✅    |
| F1 tsc          | 模块五无类型错误（exit 0）               | ✅    |
| F2 模块测试         | 15/15 通过                       | ✅    |

> 注：仓库现存 `testing-system.ts` / `VirtualListVisualizer.tsx` 的预先存在类型错误与模块五无关，不在本次治理范围。

### 10.5 模块五关键实例锚点

| 自查项                | 模块五实例位置                                                                                |
| ------------------ | -------------------------------------------------------------------------------------- |
| 19 知识点 order 连续    | [typescript-core.ts](../src/modules/typescript-core.ts) `order: 1` … `order: 19`       |
| 16 道面试题            | 知识点 17「TypeScript 面试题精选」`visualizationType: 'accordion'`，含 Q15/Q16 场景题、Q13/Q14 对比题，`defaultMode: 'flashcard'` |
| 20 道小测题（含梯度标注）     | 知识点 19「TypeScript 小测验」`visualizationType: 'quiz'`，题干前缀【记忆】【理解】【应用】【对比】【场景】【综合】         |
| 实战项目 1（6 条 checks） | 知识点 15「综合实战：手写工具类型库」`visualizationType: 'sandbox'` `language: 'typescript'`            |
| 实战项目 2（6 条 checks） | 知识点 16「综合实战：类型安全的 API 响应解析器」`visualizationType: 'sandbox'` `language: 'typescript'`     |
| 知识点速查表             | 知识点 18「TypeScript 速查表」`visualizationType: 'comparetable'`，14 行                         |
| 沙盒语言类型扩展           | [types.ts](../src/lib/types.ts) `SandboxData.language` 增加 `'typescript'`              |
| 模块测试               | [typescript-core.test.ts](../src/test/typescript-core.test.ts)                         |

***

## 十一、附录：模块一关键实例锚点

| 自查项                | 模块一实例位置                                                                              |
| ------------------ | ------------------------------------------------------------------------------------ |
| 26 知识点 order 连续    | [html-fundamentals.ts](../src/modules/html-fundamentals.ts) `order: 1` … `order: 26` |
| 16 道面试题            | 知识点 24「面试题」`visualizationType: 'accordion'`                                          |
| 15 道小测题（含梯度标注）     | 知识点 26「小测验」`visualizationType: 'quiz'`，题干前缀【理解】【应用】【对比】【场景】【综合】                      |
| 实战项目 1（9 条 checks） | 知识点 22「综合实战：搭建博客文章页」`visualizationType: 'sandbox'`                                   |
| 实战项目 2（8 条 checks） | 知识点 23「综合实战：带验证注册表单」`visualizationType: 'sandbox'`                                   |
| 知识点速查表             | 知识点 25「知识点速查表」                                                                       |
| SandboxCheck 类型定义  | [types.ts](../src/lib/types.ts) `SandboxCheck` 接口                                    |
| 任务检查清单面板           | [Sandbox.tsx](../src/components/interactive/Sandbox.tsx) `checkResults` / `useMemo`  |
| 模块测试模板             | [html-fundamentals.test.ts](../src/test/html-fundamentals.test.ts)                   |

***

**版本**：v1.0 · 基线源自模块一修复（2026-06）
**维护**：当模块一进一步升级基线时，同步更新本文件的"模块一实例值"与清单门槛。
