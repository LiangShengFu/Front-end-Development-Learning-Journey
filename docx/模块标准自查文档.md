# 模块标准自查文档

> 以模块一「HTML 基础」修复后的质量基线为基准，抽象出可复用的模块质量标准，供其它模块在新建/重构时对照自查，确保全站学习体验的一致性与有效性。

***

## 一、文档目的

本文件解决一个具体问题：**如何判断一个学习模块是否"达标"？**

模块一此前存在三类典型缺陷——评估环节形同虚设、知识呈现静态化、内容组织学科导向。经修复后，模块一在「以测促学 / 动态反馈 / 任务导向」三个维度上建立了可参照的基线。本文件把该基线抽象成 7 个自查维度 + 1 张勾选清单，使其它模块无需重复摸索即可对齐质量。

**适用对象**：所有 `src/modules/*.ts` 学习模块（模块二 CSS、模块三 JS …… 及后续新增模块）。

**使用方式**：模块开发/重构完成后，逐项对照第三章维度与第四章清单打勾；任一维度不达标即视为该模块未完成质量门禁。

***

## 二、模块一基线概览（实例对照）

修复后的模块一达成了以下量化指标，作为其它模块的**下限参考**（不是上限，鼓励超越）：

| 维度                  | 模块一实例值 | 说明                                      |
| ------------------- | ------ | --------------------------------------- |
| 知识点数量               | **26** | order 1–26 连续无跳号                        |
| 面试题数量               | **16** | 覆盖文档流/盒模型/表单验证/meta/无障碍/语义化/srcset/场景题等 |
| 小测题数量               | **15** | 含【记忆】【理解】【应用】【对比】【场景】【综合】梯度标注           |
| 综合实战项目              | **2**  | KP22 搭建博客文章页、KP23 带验证注册表单               |
| 沙盒断言 checks         | **17** | 9 + 8，分布在 2 个实战项目沙盒                     |
| 知识点速查表              | **1**  | KP25                                    |
| 类型检查 `tsc --noEmit` | **通过** | exit 0                                  |
| 模块测试                | **通过** | html-fundamentals.test.ts 全绿            |

**关键文件锚点**：

- 模块数据：[src/modules/html-fundamentals.ts](../src/modules/html-fundamentals.ts)
- 沙盒类型：[src/lib/types.ts](../src/lib/types.ts) `SandboxData` / `SandboxCheck`
- 沙盒组件：[src/components/interactive/Sandbox.tsx](../src/components/interactive/Sandbox.tsx)
- 模块测试：[src/test/html-fundamentals.test.ts](../src/test/html-fundamentals.test.ts)

***

## 三、自查维度

每个维度包含：**① 标准要求 → ② 模块一实例 → ③ 检查方法 → ④ 常见缺陷**。

### 维度 A：知识点结构与编排

**① 标准要求**

- A1. `points` 数组的 `order` 字段从 1 连续递增至 N，**无重复、无跳号、无小数**。
- A2. 每个知识点具备 `title` / `difficulty`(1–5) / `blocks` 三要素。
- A3. 难度分布合理：以 1–3 为主，进阶内容(4–5)不超过总量的 30%。
- A4. 新增/重构的知识点显式标注 `isNew: true`，便于学习者识别更新。
- A5. `blocks` 内 `id` 遵循 `p{order}-{n}` 命名规范，全模块唯一。

**② 模块一实例**

26 个知识点 order 1–26 严格连续；KP22/KP23 实战项目标注 `isNew: true`；难度集中在 1–2，仅个别 API 章节为 3。

**③ 检查方法**

```bash
# 提取所有 order 值，人工核对连续性
# （用 IDE 正则搜索 ^\s+order:\s+(\d+),）
```

或在模块测试中断言：

```ts
it('知识点序号应从 1 到 N 连续', () => {
  const orders = someModule.points.map((p) => p.order)
  for (let i = 0; i < orders.length; i++) {
    expect(orders[i]).toBe(i + 1)
  }
})
```

**④ 常见缺陷**

- 插入新知识点后未重排后续 order，导致跳号/重复（模块一曾出现 order 21 / 21.5 / 22 冲突）。
- 用小数 order（如 21.5）作为临时插入，破坏连续性约定。

***

### 维度 B：评估环节有效性（以测促学）

**① 标准要求**

- B1. **面试题 ≥ 30 题**，覆盖该模块所有核心考点，无高频考点遗漏。
- B2. **小测题 ≥ 20 题**，且分布在模块末尾的独立 QuizCard 知识点。
- B3. 题目具备**梯度**，能区分"记忆 / 理解 / 应用"三个层次：
  - 记忆层：直问定义、标签类型；
  - 理解层：概念辨析、原理说明；
  - 应用层：场景判断、代码选型、排错。
- B4. 面试题至少包含 **2 道场景题**（"遇到 X 问题你怎么排查/重构"）与 **2 道对比题**（易混淆点 A vs B）。
- B5. 每道题的 `explanation` / `content` 不仅给结论，还解释"为什么"，指向易错点。
- B6. 考点覆盖率自查：列出模块所有核心知识点，逐个标注是否有对应题目，无盲区。
- B7. **面试题答案排版样式**（对齐模块 1-7 阅读习惯，与小测题样式区分）：
  - **标题格式**：`Q{n}: 题目？`；场景题用 `Q{n}: 场景题——题目`、对比题用 `Q{n}: 对比题——题目` 前缀标注。**面试题标题不带【】梯度标注**（梯度标注仅用于小测题，避免与题干混淆）。
  - **答案分段**：用 `\n\n` 分段，不能是单段长文。先给结论/总起句，再展开。
  - **列表结构**：枚举用编号列表 `1. ` `2. `、并列要点用项目符号 `- `，至少含一处列表。
  - **收尾结论**：以 `结论：/推荐：/注意：/原则：` 等收尾，点明取舍或易错点。
  - 代码题（手写 Hook 等）用 `\n` 换行的代码块 + "关键点/原理"列表说明。

**② 模块一实例**

- 面试题 16 题：Q14 场景题（页面卡顿排查）、Q15 场景题（div 导航重构）、Q8 对比题（标准/IE 盒模型）、Q16 对比题（`<b>`/`<strong>` 等）。
- 小测题 15 题，题干前缀标注【理解】【应用】【对比】【场景】【综合】。
- 考点覆盖：文档流、盒模型、meta、viewport、语义化、defer/async、无障碍 POUR、ARIA、表单验证、srcset、空元素等均有对应题目。

**③ 检查方法**

```bash
# 统计面试题数量（匹配 Q{n}: 前缀）
# 统计小测题数量（匹配 ^\s+question:）
# 人工核对：是否存在"学了但没考"的知识点
```

**④ 常见缺陷**

- 题目全是直问定义，无法区分背书与理解（缺应用层）。
- 高频考点（如模块一的"文档流""表单验证"）无对应题目。
- 题目集中在模块末尾，章节学完无即时检验（理想态：每章末可加微测验，至少模块级总测验要全覆盖）。

***

### 维度 C：动态反馈闭环（沙盒断言）

**① 标准要求**

- C1. `SandboxData` 类型支持 `checks?: SandboxCheck[]` 字段（已由 types.ts 全局提供，各模块直接使用）。
- C2. `Sandbox.tsx` 渲染"任务检查清单"面板，对学习者代码**实时**跑断言并显示通过/未通过 + 中文教学提示（已由组件全局提供）。
- C3. 模块内**任务型练习沙盒**（尤其是实战项目）必须配置 `checks`，至少 5 条。**自由探索型沙盒**（hint 为"修改 X 观察 Y"的演练场）可不配 checks，因其教学目的是自由试错而非任务达标。区分依据：沙盒是否设定了明确的"完成目标"。
- C4. 每条 check 三要素齐全：`description`（任务要求）/ `pattern`（正则）/ `hint`（未通过时的教学引导）。
- C5. `pattern` 必须在沙盒中可正确编译（避免正则语法错误导致永远不通过）。
- C6. 断言不只是"语法存在性"检查，应体现**教学意图**（如"必须写 alt"对应无障碍教学点）。
- C7. 全部通过时显示完成态，给学习者正反馈。

**② 模块一实例**

- KP22 博客文章页沙盒：9 条 checks（charset / viewport / header / nav / main / article / img+alt / aside / footer）。
- KP23 注册表单沙盒：8 条 checks（form / text+required / email+required / password+minlength / select / button\[type=submit] / label\[for] / aria-required）。
- 每条 hint 指出"该怎么做 + 为什么"，例如 img+alt 的 hint 解释"无 alt 的图片对视障用户不存在，违反 WCAG 可感知原则"。

**③ 检查方法**

```bash
# 统计 checks 数量（匹配 ^\s+pattern:）
# 人工核对：每个配置了 checks 的沙盒，其 pattern 是否能在预期代码上通过、在错误代码上失败
```

**类型字段速查**：

```ts
export interface SandboxCheck {
  description: string  // 任务要求（展示给学习者）
  pattern: string      // 正则字符串，命中即通过
  flags?: string       // 默认 'i'
  hint: string         // 未通过时的教学提示
}
```

**④ 常见缺陷**

- 沙盒只支持"编辑-预览"，写错只能看浏览器原生报错，无教学反馈（模块一修复前的状态）。
- `pattern` 写得过严或过松：过严导致正确写法被判失败，过松导致随便写都通过。
- 只有语法检查没有语义检查（如只查"有没有 `<img>`"而不查"有没有 alt"）。

***

### 维度 D：任务导向内容组织（综合实战项目）

**① 标准要求**

- D1. 模块内至少 **1 个**（建议 2 个）综合实战知识点，放在该模块核心章节学完之后。
- D2. 实战项目必须**贯穿多个章节**的知识点，扮演"拼装图纸"角色，而非单点练习。
- D3. 每个实战项目配 `checks` 断言沙盒（接入维度 C）。
- D4. 沙盒提供**初始骨架代码**（带注释占位），降低冷启动门槛。
- D5. 项目前后有 `callout` 说明"为什么这个练习重要"与"实战后的反思"，连接工程思维。
- D6. 项目命名体现**真实开发场景**（如"搭建博客文章页""带验证的注册表单"），而非"练习 1"。

**② 模块一实例**

- KP22「搭建博客文章页」：串联 head 元信息 + 语义化骨架 + 多媒体无障碍，9 条断言，含 `callout variant="tip"` 说明练习价值。
- KP23「带验证注册表单」：串联表单控件 + 原生验证 + 无障碍 label 关联，8 条断言，含 `callout variant="warning"` 反思"前端验证的边界"（衔接后端校验）。
- 两者均提供带 `<!-- -->` 占位注释的初始骨架。

**③ 检查方法**

```bash
# 搜索实战项目知识点（title 含"实战"/"综合"）
# 核对每个实战沙盒是否配置 checks、是否有初始骨架、是否有 callout
```

**④ 常见缺陷**

- 知识按学科分类（结构→语义→多媒体→表单→API），学完仍不会"从零搭页面"。
- 没有贯穿多章的渐进式项目，知识点是离散积木缺拼装图纸。
- 实战项目变成"照抄示例"，无断言引导，无法定位薄弱环节。

***

### 维度 E：辅助学习资源

**① 标准要求**

- E1. 模块末尾含 1 个**知识点速查表**（`visualizationType: 'cheatsheet'` 或等价表格），浓缩核心 API/标签/属性。
- E2. 关键概念处使用 `callout`（`tip` / `warning` / `info`）强调易错点、最佳实践、边界条件。
- E3. 首个知识点有 `lead: true` 的引导段落，说明本章学什么、为什么学。
- E4. 代码示例遵守项目设计规范（canvas-mid 代码区、hairline 边框、等宽字体）。

**② 模块一实例**

- KP25 知识点速查表。
- 多处 callout：如表单章节的"无障碍表单要点"(tip)、KP23 的"前端验证边界"(warning)。
- 每章首段 `lead: true` 引导。

**③ 检查方法**

```bash
# 搜索 callout 使用密度（匹配 type: 'callout'）
# 确认存在 cheatsheet / 速查表知识点
```

**④ 常见缺陷**

- 全文平铺直叙，无重点强调，学习者抓不住易错点。
- 缺速查表，复习时无法快速定位。

***

### 维度 F：类型与工程质量

**① 标准要求**

- F1. `npx tsc --noEmit` 通过（exit 0），无类型错误。
- F2. 存在对应模块测试 `src/test/{module}.test.ts`，且全绿。
- F3. 测试覆盖：知识点数量、order 连续性、每个知识点三要素、可视化组件元数据。
- F4. 模块数据严格符合 `ModuleData` 类型（`points` / `blocks` / `data` 结构正确）。
- F5. 新增 `visualizationType` 时同步登记到 `visualizationMeta`（避免 types.test.ts 漂移）。
- F6. 不引入与模块无关的全局类型变更（避免污染其它模块）。

**② 模块一实例**

- `tsc --noEmit` exit 0。
- `html-fundamentals.test.ts` 14/14 通过（含 26 知识点、order 连续、三要素校验）。
- 注：`types.test.ts` 的 `visualizationMeta` (105 vs 173) 失败为**预先存在**的全局漂移，与模块一无关，需单独治理。

**③ 检查方法**

```bash
npx tsc --noEmit
npx vitest run src/test/{module}.test.ts --reporter=dot
```

**④ 常见缺陷**

- 新增知识点后忘更新测试中的 `toHaveLength(N)`，导致测试红。
- 新增可视化类型未登记 `visualizationMeta`，触发 types.test.ts 失败。
- order 用小数临时插入导致连续性测试失败。

***

## 四、自查清单（勾选式）

> 模块开发/重构完成后，逐项打勾。**任一未勾选 = 未过质量门禁。**

### A. 知识点结构

- [ ] A1. `order` 1..N 连续无重复无跳号无小数
- [ ] A2. 每个知识点有 `title` / `difficulty` / `blocks`
- [ ] A3. 难度分布合理（1–3 为主）
- [ ] A4. 新增知识点标注 `isNew: true`
- [ ] A5. block `id` 符合 `p{order}-{n}` 且全模块唯一

### B. 评估环节

- [ ] B1. 面试题 ≥ 30 题
- [ ] B2. 小测题 ≥ 20 题
- [ ] B3. 题目含记忆/理解/应用三梯度
- [ ] B4. 面试题含 ≥ 2 场景题 + ≥ 2 对比题
- [ ] B5. 每题 `explanation` 解释"为什么"
- [ ] B6. 核心考点全覆盖（无"学了没考"盲区）

### C. 动态反馈闭环

- [ ] C1. 使用 `SandboxData.checks`（类型已就绪）
- [ ] C2. 关键沙盒配置 `checks`（≥ 5 条）
- [ ] C3. 每条 check 三要素齐全（description/pattern/hint）
- [ ] C4. `pattern` 经实测：正确代码通过、错误代码失败
- [ ] C5. 断言体现教学意图（非纯语法检查）
- [ ] C6. 全部通过有完成态反馈

### D. 任务导向实战

- [ ] D1. 至少 1 个综合实战知识点（建议 2 个）
- [ ] D2. 项目贯穿多章节知识点
- [ ] D3. 配 `checks` 断言沙盒
- [ ] D4. 提供带注释占位的初始骨架
- [ ] D5. 前后有 `callout`（练习价值 + 反思）
- [ ] D6. 命名体现真实开发场景

### E. 辅助资源

- [ ] E1. 模块末尾有知识点速查表
- [ ] E2. 关键概念有 `callout` 强调
- [ ] E3. 首知识点有 `lead: true` 引导段
- [ ] E4. 代码示例符合设计规范

### F. 工程质量

- [ ] F1. `tsc --noEmit` 通过
- [ ] F2. 模块测试存在且全绿
- [ ] F3. 测试覆盖数量/order/三要素
- [ ] F4. 数据符合 `ModuleData` 类型
- [ ] F5. 新增 `visualizationType` 已登记 `visualizationMeta`
- [ ] F6. 未引入无关全局类型变更
- [ ] F7. 沙盒 `language` 值在 `SandboxData.language` 类型联合内（`'js' | 'html' | 'css' | 'typescript'`），新增语言须同步扩展类型联合（避免对象字面量多余属性报错）
- [ ] F8. 可视化组件多样性：全模块使用 ≥ 8 种不同 `visualizationType`（避免单一组件堆砌，提升学习体验）
- [ ] F9. 测试断言阈值与标准一致：模块测试中的数量/比例断言（面试题 ≥ 30、小测题 ≥ 20、checks ≥ 5、进阶占比 ≤ 30%、场景题 ≥ 2、对比题 ≥ 2）须与第四章清单保持一致，避免"标准说 30、测试只测 12"的阈值漂移
- [ ] F10. 面试题 Accordion 在题量 ≥ 20 时启用 `defaultMode: 'flashcard'`（避免长列表滚动疲劳，支持一题一屏翻转 + 上下题导航），学习者仍可切回 list 模式
- [ ] F11. `knowledgePointCount` / `visualizationCount` 元数据字段与实际 `points.length` / 可视化块数量一致（避免首页/列表展示与详情页不符）

***

## 五、应用流程（如何把本标准用到新模块）

1. **新建模块骨架**：复制 `html-fundamentals.ts` 的结构（顶部 import、`ModuleData` 形状），改 `number/title/slug/stage/icon`。
2. **编写知识点**：按 `order` 1..N 顺序编写，遵循 A 系列标准。
3. **穿插实战项目**：在核心章节后插入综合实战 KP，配置 `checks` 沙盒（D + C 标准）。
4. **编写评估题**：先列出全部核心考点，再为每个考点配题，确保 B6 无盲区；题目前缀标注梯度。
5. **补速查表与 callout**：模块末尾加速查表，关键处加 callout（E 标准）。
6. **编写模块测试**：参照 `html-fundamentals.test.ts`，覆盖数量/order 连续/三要素。测试中的数量/比例断言须与第四章清单阈值一致（F9），避免"标准说 30、测试只测 12"。
7. **跑质量门禁**：
   ```bash
   npx tsc --noEmit
   npx vitest run src/test/{module}.test.ts --reporter=dot
   ```
8. **对照第四章清单逐项打勾**，未勾项回到对应维度修复。
9. **登记可视化类型**：若引入新 `visualizationType`，同步更新 `visualizationMeta` 与 `types.test.ts`。
10. **登记验收记录**：在第六章「模块验收记录」追加该模块的指标表与关键修复项，便于横向对比与回归追踪。

***

## 六、模块验收记录

> 每个模块按本标准完成自查后，在此登记验收结果与关键指标，便于横向对比与回归追踪。

### 模块 08：React 基础与核心能力 ✅ 已通过（2026-06-27）

| 维度                   | 模块八实例值 | 说明                                                              |
| -------------------- | ------- | --------------------------------------------------------------- |
| 知识点数量                | **24**  | order 1–24 连续无跳号；原 22 个，本次新增 KP21（综合实战·数据获取）+ KP23（速查表）          |
| 面试题数量                | **32**  | Accordion 模式 `flashcard`；覆盖核心概念/Hooks/状态管理/性能/React 19/场景排查/方案对比，含 5 场景题 + 8 对比题，标注【记忆】【理解】【应用】【对比】【场景】梯度 |
| 小测题数量                | **20**  | QuizCard；题目含【记忆】【理解】【应用】【对比】【场景】【综合】梯度标注（标注率 100%）              |
| 综合实战项目               | **2**   | KP11 受控注册表单（7 checks）、KP21 Hooks 数据获取与列表（6 checks）              |
| 沙盒断言 checks          | **13**  | 7 + 6，分布在 2 个综合实战沙盒                                             |
| 知识点速查表               | **1**   | KP23，table 块 25 行                                               |
| isNew 标注             | **6**   | KP11/KP17/KP21/KP22/KP23/KP24（综合实战、React 19、面试题扩充、速查表、小测扩充）     |
| block id 命名           | `p{order}-{n}` | 已从 `r-p{order}-{n}` 统一为 `p{order}-{n}`，全模块唯一                    |
| 类型检查 `tsc --noEmit`  | **通过**  | exit 0                                                          |
| 模块测试                 | **通过**  | `src/test/react-fundamentals.test.ts` 16/16 全绿                   |

**关键修复项**：

1. **block id 规范化**（A5）：将原 `r-p{order}-{n}` 前缀统一为 `p{order}-{n}`，与其它模块对齐。
2. **综合实战沙盒**（C/D）：把原 KP11「表单处理实战」从纯代码示例升级为带 7 条断言的受控注册表单沙盒；新增 KP21「综合实战：Hooks 数据获取与列表」沙盒（6 条断言，覆盖三态/loading/error/key/竞态清理）。
3. **面试题扩充**（B1/B4）：6 → 32 题，新增 useState/useReducer、Context/Redux、受控/非受控、R17/R18、SSR/CSR/SSG/ISR、状态管理选型、HOC vs Hook、Fiber、Suspense/ErrorBoundary、React 19 Actions 等，含 5 场景题（列表串行/effect 死循环/卸载 setState/列表卡顿/父组件重渲染）+ 8 对比题。
4. **小测题扩充**（B2/B3）：6 → 20 题，全部标注梯度（记忆 2 / 理解 4 / 对比 4 / 应用 3 / 场景 3 / 综合 4）。
5. **速查表**（E1）：新增 KP23，25 行覆盖 JSX/虚拟 DOM/Hooks 全家桶/受控非受控/Context/状态管理选型/React 19/性能/并发/Suspense/陷阱。
6. **isNew 标注**（A4）：为综合实战、React 19、扩充的面试题/速查表/小测标注 `isNew: true`。
7. **闪卡模式**（F10）：面试题 Accordion 启用 `defaultMode: 'flashcard'`，32 题一题一屏翻转，避免长列表疲劳。
8. **元数据同步**（F11）：`knowledgePointCount` 22→24、`visualizationCount` 22→23，同步更新 `src/modules/react-fundamentals.ts` 与 `src/lib/modules.ts`。
9. **模块测试**（F2/F3/F9）：新建 `src/test/react-fundamentals.test.ts`（16 用例），覆盖元数据/order 连续/三要素/id 唯一/可视化多样性/难度分布/沙盒 checks/面试题题量与场景对比题/小测题量与梯度标注/速查表/isNew/综合实战数量，阈值与本标准一致。

**关键文件锚点**：

- 模块数据：[src/modules/react-fundamentals.ts](../src/modules/react-fundamentals.ts)
- 模块测试：[src/test/react-fundamentals.test.ts](../src/test/react-fundamentals.test.ts)
- 模块注册表：[src/lib/modules.ts](../src/lib/modules.ts)（已同步 knowledgePointCount/visualizationCount）

**遗留与说明**：

- `src/test/types.test.ts` 的 `visualizationMeta` (105 vs 173) 失败为**预先存在**的全局漂移（git stash 验证：模块八改动前后均失败），与模块八无关，需单独治理，不阻塞模块八验收。

***

### 模块 09：React 进阶与生态体系（系统检查与修复记录）

**检查结果汇总**（参照第四章自查清单逐项核对）：

| 检查项                | 修复前 | 修复后 | 说明                                                                 |
|----------------------|--------|--------|----------------------------------------------------------------------|
| 知识点数量            | 18     | **21** | 新增 KP19 综合实战 / KP20 面试题 / KP21 速查表                          |
| block id 命名（A5）   | `ra-p{order}` | `p{order}` | 全部重命名，去除模块前缀，与其它模块对齐                              |
| icon 元数据（F11）    | `⚛️`（与 modules.ts `09` 不符） | `09` | 模块文件与注册表一致                                                  |
| 面试题数量（B1/B4/B7）| **缺失** | **32** | Accordion `flashcard`；含 6 场景题 + 4 对比题；标题 `Q{n}: ...`+`场景题——`/`对比题——` 前缀（无【】梯度标注）；答案 `\n\n` 分段 + 编号/项目符号列表 + `结论：/推荐：/注意：` 收尾 |
| 小测题数量（B2/B3）   | 10     | **20** | 全部标注【记忆】【理解】【应用】【对比】【场景】【综合】梯度（标注率 100%）   |
| 综合实战沙盒（C/D）   | **缺失** | **1**  | KP19 列表性能优化，6 条 checks（memo/useMemo/useCallback/useTransition/key/isPending）|
| 沙盒断言 checks       | 0      | **6**  | 分布在 KP19 综合实战沙盒                                              |
| 知识点速查表（E1）    | **缺失** | **1**  | KP21，table 块 24 行                                                  |
| isNew 标注（A4）      | 无     | **3**  | KP19/KP20/KP21（综合实战、面试题、速查表）                              |
| 可视化组件多样性（F8）| 13 种  | **14 种** | 新增 sandbox；≥8 达标                                                  |
| visualizationCount（F11）| 文件 15 / 注册表 9（不符） | **15 / 15** | 同步一致                                                              |
| 类型检查 `tsc --noEmit`| —     | **通过** | exit 0                                                               |
| 模块测试（F2/F3/F9）  | 无     | **通过** | `src/test/react-advanced.test.ts` 17/17 全绿                          |

**关键修复项**：

1. **block id 规范化**（A5）：将原 `ra-p{order}-{n}` 前缀统一为 `p{order}-{n}`，与其它模块对齐。
2. **元数据同步**（F11）：修复 icon `⚛️`→`09`、`knowledgePointCount` 18→21、`visualizationCount`（注册表 9→15 与文件一致），同步 `src/modules/react-advanced.ts` 与 `src/lib/modules.ts`。
3. **综合实战沙盒**（C/D）：新增 KP19「综合实战：列表性能优化」沙盒（6 条断言，覆盖 React.memo/useMemo/useCallback/useTransition/稳定 key/isPending），把五个性能手段串成一个真实场景。
4. **面试题扩充**（B1/B4/B7）：从无到 32 题，覆盖 Fiber/Diff/并发/性能/状态管理选型/Suspense/ErrorBoundary/Hooks 进阶/React 19，含 6 场景题（长列表卡顿/memo 失效/effect 死循环/卸载 setState/Profiler 重渲染/状态管理选型）+ 4 对比题（useTransition vs useDeferredValue、Zustand/RTK/Jotai、React Query vs Redux、CSR/SSR/SSG/ISR）。标题统一 `Q{n}: ...`+前缀（无【】梯度标注），答案 `\n\n` 分段 + 列表 + 收尾结论，对齐模块 1-7 与 B7 标准。
5. **小测题扩充**（B2/B3）：10 → 20 题，全部标注梯度（记忆 2 / 理解 6 / 对比 4 / 应用 2 / 场景 2 / 综合 4）。
6. **速查表**（E1）：新增 KP21，24 行覆盖 Fiber/Diff/并发 Hooks/性能/状态管理/Suspense/Server Components/React 19/选型。
7. **isNew 标注**（A4）：为综合实战、面试题、速查表标注 `isNew: true`。
8. **闪卡模式**（F10）：面试题 Accordion 启用 `defaultMode: 'flashcard'`，32 题一题一屏翻转。
9. **模块测试**（F2/F3/F9）：新建 `src/test/react-advanced.test.ts`（17 用例），覆盖元数据/order 连续/三要素/id 唯一/**id 命名规范（A5）**/可视化多样性/难度分布/沙盒 checks/面试题题量+场景对比题+答案排版（B7）/小测题量+梯度/速查表/isNew/综合实战数量，阈值与本标准一致。

**关键文件锚点**：

- 模块数据：[src/modules/react-advanced.ts](../src/modules/react-advanced.ts)
- 模块测试：[src/test/react-advanced.test.ts](../src/test/react-advanced.test.ts)
- 模块注册表：[src/lib/modules.ts](../src/lib/modules.ts)（已同步 knowledgePointCount/visualizationCount/icon）

**遗留与说明**：

- `src/test/types.test.ts` 的 `visualizationMeta` 计数失败为**预先存在**的全局漂移（git stash 验证：模块九改动前后均失败），与模块九无关，不阻塞模块九验收。该测试列出全局所有可视化类型，需单独治理（注册表与 expectedTypes 列表同步）。
- 模块九通过本文档定义的所有适用检查标准（A4/A5/B1-B7/C/D/E1/F2/F3/F8/F9/F10/F11/F12）。

### 模块 10：Next.js 与 SSR/SSG 全栈 ✅ 已通过（2026-06-27）

| 维度                   | 模块十实例值 | 说明                                                              |
| -------------------- | ------- | --------------------------------------------------------------- |
| 知识点数量                | **19**  | order 1–19 连续无跳号；原 16 个，本次新增 KP17（综合实战·Server Action）+ KP18（面试题）+ KP19（速查表） |
| 面试题数量                | **32**  | Accordion 模式 `flashcard`；覆盖渲染模式/App Router/Server-Client 边界/Server Action/缓存/Middleware/SEO/场景排查/方案对比，含 12 场景题 + 6 对比题，标题 `Q{n}: ...`+前缀（无【】梯度标注） |
| 小测题数量                | **20**  | QuizCard；题目含【记忆】【理解】【应用】【对比】【场景】【综合】梯度标注（标注率 100%）              |
| 综合实战项目               | **1**   | KP17 Server Action 表单提交（6 checks）                                |
| 沙盒断言 checks          | **6**   | 分布在 1 个综合实战沙盒（use server/Zod/safeParse/revalidatePath/redirect/useActionState） |
| 知识点速查表               | **1**   | KP19，table 块 24 行                                               |
| isNew 标注（A4）         | **3**   | KP17/KP18/KP19（综合实战、面试题、速查表）                                    |
| block id 命名（A5）       | `p{order}-{n}` | 已从 `nx-p{order}-{n}` 统一为 `p{order}-{n}`，全模块唯一                    |
| 可视化组件多样性（F8）         | **13 种** | render-mode-comparator/comparetable/rsc-payload-flow/server-action-sandbox/route-vs-action-decider/codestepper/architecture/middleware-flow-explorer/framework-decision-wizard/islands-arch-demo/accordion/quiz/sandbox；≥8 达标 |
| visualizationCount（F11）| 文件 16 / 注册表 8（不符） | **18 / 18** 同步一致                                                |
| knowledgePointCount（F11）| 文件 16 / 注册表 16（不符） | **19 / 19** 同步一致                                                |
| 类型检查 `tsc --noEmit`  | **通过**  | exit 0                                                          |
| 模块测试（F2/F3/F9）       | **通过**  | `src/test/nextjs-ssr.test.ts` 18/18 全绿                          |

**关键修复项**：

1. **block id 规范化**（A5）：将原 `nx-p{order}-{n}` 前缀统一为 `p{order}-{n}`，与其它模块对齐，全模块唯一。
2. **元数据同步**（F11）：修复 `knowledgePointCount` 16→19（文件与注册表）、`visualizationCount` 注册表 8→18 与文件一致（文件原 16→18，补计 KP17 sandbox + KP18 accordion 两个新增 demo），同步 `src/modules/nextjs-ssr.ts` 与 `src/lib/modules.ts`，并更新头部注释。
3. **综合实战沙盒**（C/D）：新增 KP17「综合实战：Server Action 表单提交」沙盒（6 条断言，覆盖 "use server" 标记/Zod schema 校验/safeParse 错误返回/revalidatePath 缓存刷新/redirect 重定向/useActionState 状态接收），把 Server Action 全闭环焊在一起；前后配 `callout variant="tip"` 说明练习价值。
4. **面试题扩充**（B1/B4/B7/F10）：从无到 32 题，覆盖 SSR/SSG/ISR/CSR、App/Pages Router、Server/Client 边界、Server Action、缓存四层、Middleware、Image、SEO、Streaming、routeRules、部署等，含 12 场景题（商品页渲染选型/表单提交未更新/LCP 优化/Pages→App 迁移/Server Component 报错/ISR revalidate 选时/客户端取服务端数据/SEO 优化/i18n/bundle 优化/redirect 报错/技术选型）+ 6 对比题（revalidatePath vs revalidateTag vs router.refresh、getServerSideProps vs RSC、Route Handler vs Server Action、Next.js vs Nuxt、next/dynamic vs React.lazy、SSG vs ISR）。标题统一 `Q{n}: ...`+前缀（无【】梯度标注），答案 `\n\n` 分段 + 列表 + 收尾结论，对齐 B7 标准；启用 `defaultMode: 'flashcard'`。
5. **小测题扩充**（B2/B3）：10 → 20 题，全部标注梯度（记忆 4 / 理解 6 / 对比 4 / 应用 2 / 场景 2 / 综合 2）。
6. **速查表**（E1）：新增 KP19，24 行覆盖 CSR/SSR/SSG/ISR/App Router/Pages Router/Server-Client Component/Server Action/Route Handler/Middleware/fetch 缓存/revalidatePath/revalidateTag/layout/template/generateMetadata/next-image/next-font/next-dynamic/routeRules/Streaming/useActionState/部署。
7. **isNew 标注**（A4）：为综合实战、面试题、速查表标注 `isNew: true`。
8. **模块测试**（F2/F3/F9）：新建 `src/test/nextjs-ssr.test.ts`（18 用例），覆盖元数据/order 连续/三要素/id 唯一/**id 命名规范（A5）**/可视化多样性/难度分布/进阶占比/沙盒 checks/面试题题量+场景对比题+答案排版（B7）/小测题量+梯度/速查表/isNew/综合实战数量+callout，阈值与本标准一致。

**关键文件锚点**：

- 模块数据：[src/modules/nextjs-ssr.ts](../src/modules/nextjs-ssr.ts)
- 模块测试：[src/test/nextjs-ssr.test.ts](../src/test/nextjs-ssr.test.ts)
- 模块注册表：[src/lib/modules.ts](../src/lib/modules.ts)（已同步 knowledgePointCount/visualizationCount）

**遗留与说明**：

- `src/test/types.test.ts` 的 `visualizationMeta` 计数失败为**预先存在**的全局漂移，与模块十无关，不阻塞模块十验收。
- 模块十通过本文档定义的所有适用检查标准（A1-A5/B1-B7/C1-C7/D1-D6/E1-E4/F1-F11）。

### 模块 11：Vue.js 核心基础 ✅ 已通过（2026-06-27）

| 维度                   | 模块十一实例值 | 说明                                                              |
| -------------------- | ------- | --------------------------------------------------------------- |
| 知识点数量                | **20**  | order 1–20 连续无跳号；原 17 个，本次新增 KP18（综合实战·useFetch 封装）+ KP19（面试题）+ KP20（速查表） |
| 面试题数量                | **35**  | Accordion 模式 `flashcard`；覆盖响应式原理/Composition API/组件通信/Pinia/Vue Router/编译优化/性能/场景排查/方案对比，含 9 场景题 + 5 对比题，标题 `Q{n}: ...`+前缀（无【】梯度标注） |
| 小测题数量                | **20**  | QuizCard；题目含【记忆】【理解】【应用】【对比】【场景】【综合】梯度标注（标注率 100%）              |
| 综合实战项目               | **1**   | KP18 组合式函数 useFetch 封装（7 checks）                                 |
| 沙盒断言 checks          | **7**   | 分布在 1 个综合实战沙盒（ref/computed/AbortController/retry/onMounted/watch/onBeforeUnmount） |
| 知识点速查表               | **1**   | KP20，table 块 24 行                                               |
| isNew 标注（A4）         | **3**   | KP18/KP19/KP20（综合实战、面试题、速查表）                                    |
| block id 命名（A5）       | `p{order}-{n}` | 已从 `vue-p{order}-{n}` 统一为 `p{order}-{n}`，全模块唯一                    |
| 可视化组件多样性（F8）         | **13 种** | knowledgegraph/comparetable/proxy-reactivity-tracker/codestepper/vue-component-sandbox/define-emits-workshop/accordion/timeline/pinia-store-explorer/patch-flag-visualizer/architecture/quiz/sandbox；≥8 达标 |
| visualizationCount（F11）| 文件 17 / 注册表 17（不符） | **23 / 23** 同步一致                                                |
| knowledgePointCount（F11）| 文件 17 / 注册表 17 | **20 / 20** 同步一致                                                |
| 类型检查 `tsc --noEmit`  | **通过**  | exit 0                                                          |
| 模块测试（F2/F3/F9）       | **通过**  | `src/test/vue-fundamentals.test.ts` 18/18 全绿                     |

**关键修复项**：

1. **block id 规范化**（A5）：将原 `vue-p{order}-{n}` 前缀统一为 `p{order}-{n}`，与其它模块对齐，全模块唯一。
2. **元数据同步**（F11）：修复 `knowledgePointCount` 17→20（文件与注册表）、`visualizationCount` 17→23（文件原 17 漏计 6 个 demo，实际 21 个 + 新增 KP18 sandbox + KP19 accordion = 23），同步 `src/modules/vue-fundamentals.ts` 与 `src/lib/modules.ts`，并更新头部注释。
3. **综合实战沙盒**（C/D）：新增 KP18「综合实战：组合式函数 useFetch 封装」沙盒（7 条断言，覆盖 ref 包装/computed 派生/AbortController 取消/retry 暴露/onMounted 首次/watch 监听/onBeforeUnmount 收尾），把 Composition API 全闭环焊在一起；前后配 `callout variant="tip"` 说明练习价值。
4. **面试题扩充**（B1/B4/B7/F10）：从无到 35 题，覆盖响应式原理/ref vs reactive/computed vs watch/watch vs watchEffect/Composition vs Options/script setup/编译宏/组件通信/provide-inject/插槽/Pinia/Vue Router/Teleport/Suspense/v-if vs v-show/key 等，含 9 场景题（商品列表性能/表单不更新/首屏白屏/首次触发副作用/状态共享选型/Modal 裁剪/路由切换请求报错/逻辑复用重构/i18n）+ 5 对比题（Vue 3 vs React/Pinia vs Vuex/Composition vs Options/computed vs methods/provide-inject vs Pinia）。标题统一 `Q{n}: ...`+前缀（无【】梯度标注），答案 `\n\n` 分段 + 列表 + 收尾结论，对齐 B7 标准；启用 `defaultMode: 'flashcard'`。
5. **小测题扩充**（B2/B3）：10 → 20 题，全部标注梯度（记忆 5 / 理解 5 / 对比 4 / 应用 2 / 场景 2 / 综合 2）。
6. **速查表**（E1）：新增 KP20，24 行覆盖响应式原理/ref/reactive/computed/watch/watchEffect/script setup/defineProps/defineEmits/Props-Emits/v-model/provide-inject/插槽/Teleport/Suspense/生命周期/Vue Router 4/动态路由/Pinia/storeToRefs/PatchFlag/静态提升/v-if vs v-show/key。
7. **isNew 标注**（A4）：为综合实战、面试题、速查表标注 `isNew: true`。
8. **模块测试**（F2/F3/F9）：新建 `src/test/vue-fundamentals.test.ts`（18 用例），覆盖元数据/order 连续/三要素/id 唯一/**id 命名规范（A5）**/可视化多样性/难度分布/进阶占比/沙盒 checks/面试题题量+场景对比题+答案排版（B7）/小测题量+梯度/速查表/isNew/综合实战数量+callout，阈值与本标准一致。

**关键文件锚点**：

- 模块数据：[src/modules/vue-fundamentals.ts](../src/modules/vue-fundamentals.ts)
- 模块测试：[src/test/vue-fundamentals.test.ts](../src/test/vue-fundamentals.test.ts)
- 模块注册表：[src/lib/modules.ts](../src/lib/modules.ts)（已同步 knowledgePointCount/visualizationCount）

**遗留与说明**：

- `src/test/types.test.ts` 的 `visualizationMeta` 计数失败为**预先存在**的全局漂移，与模块十一无关，不阻塞模块十一验收。
- 模块十一通过本文档定义的所有适用检查标准（A1-A5/B1-B7/C1-C7/D1-D6/E1-E4/F1-F11）。

### 模块 12：Vue 进阶与 Nuxt 全栈 ✅ 已通过（2026-06-27）

| 维度                   | 模块十二实例值 | 说明                                                              |
| -------------------- | ------- | --------------------------------------------------------------- |
| 知识点数量                | **17**  | order 1–17 连续无跳号；原 15 个，本次新增 KP15（综合实战·useMouseTracker 封装）+ KP16（面试题），原小测题重编号为 KP17 |
| 面试题数量                | **32**  | Accordion 模式 `flashcard`；覆盖响应式原理/Composable/自定义指令/Nuxt 混合渲染/Pinia/KeepAlive/编译优化/性能/Teleport/Suspense，含 8 场景题 + 4 对比题，标题 `Q{n}: ...`+前缀（无【】梯度标注） |
| 小测题数量                | **20**  | QuizCard；题目含【记忆】【理解】【应用】【对比】【场景】【综合】梯度标注（标注率 100%，分布 4/5/4/4/2/1） |
| 综合实战项目               | **1**   | KP15 Composable useMouseTracker 封装（7 checks）                    |
| 沙盒断言 checks          | **7**   | 分布在 1 个综合实战沙盒（ref 包装/computed 节流/onMounted 注册/removeEventListener 清理/onUnmounted/return 暴露/参数化节流时间） |
| 知识点速查表               | **1**   | KP14，accordion 6 项（响应式 API/Composable 约定/自定义指令钩子/Nuxt routeRules/KeepAlive 属性/性能优化速记） |
| isNew 标注（A4）         | **2**   | KP15/KP16（综合实战、面试题）                                             |
| block id 命名（A5）       | `p{order}-{n}` | 已从 `va-p{order}-{n}` 统一为 `p{order}-{n}`，全模块唯一                    |
| 难度分布                 | 1×2 / 2×3 / 3×9 / 4×3 | 4 个不同等级（≥3 达标）；进阶（4-5）占比 3/17≈17.6%（≤30% 达标）             |
| 可视化组件多样性（F8）         | **14 种** | knowledgegraph/proxy-reactivity-tracker/comparetable/composable-flow-visualizer/custom-directive-workbench/nuxt-hybrid-render-studio/pinia-store-explorer/architecture/patch-flag-visualizer/timeline/keepalive-cache-simulator/accordion/sandbox/quiz；≥8 达标 |
| visualizationCount（F11）| 文件 17 / 注册表 17 | **17 / 17** 同步一致                                                |
| knowledgePointCount（F11）| 文件 17 / 注册表 17 | **17 / 17** 同步一致                                                |
| 类型检查 `tsc --noEmit`  | **通过**  | exit 0                                                          |
| 模块测试（F2/F3/F9）       | **通过**  | `src/test/vue-advanced-nuxt.test.ts` 18/18 全绿                     |

**关键修复项**：

1. **block id 规范化**（A5）：将原 `va-p{order}-{n}` 前缀统一为 `p{order}-{n}`，与其它模块对齐，全模块唯一。
2. **元数据同步**（F11）：修复 `knowledgePointCount` 15→17、`visualizationCount` 15→17（新增 KP15 sandbox + KP16 accordion），同步 `src/modules/vue-advanced-nuxt.ts` 与 `src/lib/modules.ts`，并更新头部注释（含 17 个 KP 编排清单）。
3. **综合实战沙盒**（C/D）：新增 KP15「综合实战：Composable useMouseTracker 封装」沙盒（7 条断言，覆盖 ref 包装 x/y、computed 节流 throttled、onMounted 注册监听、removeEventListener 清理、onUnmounted 收尾、return 暴露、参数化节流时间），把 Composable「状态 + 副作用 + 清理 + 参数化」四件套焊在一起；前后配 `callout variant="tip"`（练习价值）+ `callout variant="warning"`（onScopeDispose vs onUnmounted 边界反思）。
4. **面试题扩充**（B1/B4/B7/F10）：从无到 32 题，覆盖响应式原理/track-trigger-effect/WeakMap/activeEffect/Composable vs Mixins/自定义指令/Nuxt routeRules/useFetch SSR 双重执行/PatchFlag/shallowRef/KeepAlive/Provide-Inject/effectScope/toRefs/Nuxt plugins/SSR vs SSG/watch vs watchEffect/Pinia 异步 action/LCP 优化等，含 8 场景题（Composable 异步竞态/KeepAlive 表单状态错乱/Nuxt SSR window 未定义/首屏慢优化/Pinia 持久化/Vue2→Vue3 迁移/i18n SSR 闪烁/ routeRules 综合配置）+ 4 对比题（Vue 3 vs Vue 2/SSR vs SSG vs ISR vs SPA/Pinia vs Vuex/Options vs Composition）。标题统一 `Q{n}: ...`+前缀（无【】梯度标注），答案 `\n\n` 分段 + 列表 + 收尾结论，对齐 B7 标准；启用 `defaultMode: 'flashcard'`。
5. **小测题扩充**（B2/B3）：10 → 20 题，全部标注梯度（记忆 4 / 理解 5 / 对比 4 / 应用 4 / 场景 2 / 综合 1），每题 4 选项 + 解析。
6. **isNew 标注**（A4）：为综合实战、面试题标注 `isNew: true`。
7. **知识点顺序调整**：将原 KP15（小测题）后移为 KP17，腾出 KP15（综合实战）+ KP16（面试题），形成「章节学习 → 速查 → 综合实战 → 面试 → 小测」的递进闭环。
8. **模块测试**（F2/F3/F9）：新建 `src/test/vue-advanced-nuxt.test.ts`（18 用例），覆盖元数据/order 连续/三要素/id 唯一/**id 命名规范（A5）**/可视化多样性/难度分布/进阶占比/沙盒 checks（≥5 且正则可编译）/面试题题量+场景对比题+答案排版（B7）/小测题量+梯度/速查表/isNew/综合实战数量+callout，阈值与本标准一致。

**关键文件锚点**：

- 模块数据：[src/modules/vue-advanced-nuxt.ts](../src/modules/vue-advanced-nuxt.ts)
- 模块测试：[src/test/vue-advanced-nuxt.test.ts](../src/test/vue-advanced-nuxt.test.ts)
- 模块注册表：[src/lib/modules.ts](../src/lib/modules.ts)（已同步 knowledgePointCount/visualizationCount）

**遗留与说明**：

- `src/test/types.test.ts` 的 `visualizationMeta` 计数失败（105 vs 173）为**预先存在**的全局漂移（stash 验证：与本模块改动无关），不阻塞模块十二验收。
- 模块十二通过本文档定义的所有适用检查标准（A1-A5/B1-B7/C1-C7/D1-D6/E1-E4/F1-F11）。

---

### 模块 24：数据结构与前端算法 ✅ 已通过（2026-06-27）

| 维度                   | 模块二十四实例值 | 说明                                                              |
| -------------------- | ------- | --------------------------------------------------------------- |
| 知识点数量                | **30**  | order 1–30 连续无跳号；按「基础前置→核心数据结构→算法思想→前端专项→面试实战」分层递进编排，原 18 个扩至 30 个 |
| 面试题数量                | **24**  | Accordion 模式 `flashcard`；覆盖 shift/unshift 复杂度/哈希冲突/快排优化/二叉树迭代遍历/BFS 最短路/DP vs 贪心/0-1 vs 完全背包/LRU vs LFU/Vue3 Diff LIS/防抖节流/WeakMap 深拷贝/Promise 限流/虚拟列表/数组转树/KMP/栈实现队列/单调栈/并查集/TopK 堆/单调队列/Trie 优势/邻接矩阵表/拓扑排序/rIC 动画，标题 `Q{n}: ...` |
| 小测题数量                | **20**  | QuizCard；题目含【记忆】【理解】【应用】【对比】【场景】【综合】梯度标注（标注率 100%，分布 2/3/4/3/4/4） |
| 综合实战项目               | **0**   | 本模块为算法知识模块，无沙盒实战（与模块12不同），改为 KP25 刷题路线 + KP26 答题方法论 + KP27 易错点汇总构成实战体系 |
| 知识点速查表               | **1**   | KP28，含 12 张 flipcard 速查卡 + 2 张 table（数据结构复杂度速查 / 算法思想一句话） |
| isNew 标注（A4）         | **30**  | 全模块知识点统一标注 `isNew: true`（按新大纲全面重写） |
| block id 命名（A5）       | `p{order}-{n}` | 全模块唯一，符合规范 |
| 难度分布                 | 1×5 / 2×19 / 3×6 | 3 个不同等级（≥3 达标）；进阶（4-5）占比 0/30=0%（≤30% 达标） |
| 可视化组件多样性（F8）         | **11 种** | knowledgegraph/stack-queue-visualizer/linked-list-stepper/comparetable/binary-tree-walker/bfs-path-finder/sorting-race-arena/codestepper/handwriting-challenge/flipcard/accordion/quiz；≥8 达标 |
| visualizationCount（F11）| 文件 16 / 注册表 16 | **16 / 16** 同步一致                                                |
| knowledgePointCount（F11）| 文件 30 / 注册表 30 | **30 / 30** 同步一致                                                |
| 类型检查 `tsc --noEmit`  | **通过**  | exit 0                                                          |
| 模块测试（F2/F3/F9）       | **通过**  | `src/test/data-structure-algorithm.test.ts` 20/20 全绿              |

**关键修复项**：

1. **大纲重构**：原内容平铺混乱、无学习梯度，重写为「基础前置（KP1-KP3）→ 基础数据结构（KP4-KP6）→ 进阶数据结构（KP7-KP11）→ 核心算法思想（KP12-KP19）→ 前端专项算法（KP20-KP24）→ 面试与实战体系（KP25-KP30）」六篇阶梯结构。
2. **结构统一**：每个知识点严格遵循「核心原理 → 代码实现 → 复杂度分析 → 前端场景 → 边界易错 → 典型例题」六段式，解决原内容重代码、轻原理、缺场景的问题。
3. **新增数据结构**：补齐 Trie 字典树（KP10）、并查集（KP11），原内容完全缺失。
4. **新增算法思想**：补齐回溯（KP15）、分治（KP16）、贪心（KP17）、动态规划（KP18）、位运算（KP19），原内容仅含排序查找。
5. **前端专项算法篇**：新增 KP20-KP24（手写题/浏览器渲染/框架算法/业务场景/字符串），深度绑定 JS 语言特性与前端业务/浏览器/框架场景（Vue3 Diff/Fiber/虚拟列表/Promise 限流/防抖节流/数组转树等）。
6. **面试与实战体系篇**：新增 KP25 刷题路线（按 LC 难度梯度 + 前端优先级）、KP26 答题方法论（五步法 + 复杂度分析 + 边界确认 + 优化思路 + 代码规范）、KP27 易错点汇总。
7. **面试题精选**（B1/B4/B7/F10）：新增 24 题，覆盖原理/对比/场景/手写四类，启用 `defaultMode: 'flashcard'`，答案 `\n\n` 分段 + 列表 + 收尾结论。
8. **小测题扩充**（B2/B3）：新增 20 题，全部标注梯度（记忆 2 / 理解 3 / 应用 4 / 对比 3 / 场景 4 / 综合 4），每题 4 选项 + 解析。
9. **速查表**（E1）：新增 KP28，12 张 flipcard 速查卡 + 2 张 table（数据结构复杂度 / 算法思想一句话）。
10. **元数据同步**（F11）：`knowledgePointCount` 18→30、`visualizationCount` 6→16，同步 `src/modules/data-structure-algorithm.ts` 与 `src/lib/modules.ts`，并更新头部注释。
11. **模块测试**（F2/F3/F9）：新建 `src/test/data-structure-algorithm.test.ts`（20 用例），覆盖元数据/order 连续/三要素/id 唯一/id 命名规范/可视化多样性/难度分布/进阶占比/**分层递进大纲完整性**（验证 6 篇 30 个知识点主题词全覆盖）/面试题题量+答案排版（B7）/小测题量+梯度/速查表 flipcard+table/isNew/算法专属可视化组件/codestepper 覆盖核心算法。

**关键文件锚点**：

- 模块数据：[src/modules/data-structure-algorithm.ts](../src/modules/data-structure-algorithm.ts)
- 模块测试：[src/test/data-structure-algorithm.test.ts](../src/test/data-structure-algorithm.test.ts)
- 模块注册表：[src/lib/modules.ts](../src/lib/modules.ts)（已同步 knowledgePointCount/visualizationCount）

**遗留与说明**：

- `src/test/types.test.ts` 的 `visualizationMeta` 计数失败（105 vs 173）为**预先存在**的全局漂移（stash 验证：与本模块改动无关），不阻塞模块二十四验收。
- 模块二十四通过本文档定义的所有适用检查标准（A1-A5/B1-B7/C1-C7/D1-D6/E1-E4/F1-F11）。

