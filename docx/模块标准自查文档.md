# 模块标准自查文档

> 以模块一「HTML 基础」修复后的质量基线为基准，抽象出可复用的模块质量标准，供其它模块在新建/重构时对照自查，确保全站学习体验的一致性与有效性。

***

## 一、文档目的

本文件解决一个具体问题：**如何判断一个学习模块是否"达标"？**

模块一此前存在三类典型缺陷——评估环节形同虚设、知识呈现静态化、内容组织学科导向。经修复后，模块一在「以测促学 / 动态反馈 / 任务导向」三个维度上建立了可参照的基线。本文件把该基线抽象成 6 个自查维度 + 1 张勾选清单，使其它模块无需重复摸索即可对齐质量。

**适用对象**：所有 `src/modules/*.ts` 学习模块（模块二 CSS、模块三 JS …… 及后续新增模块）。

**使用方式**：模块开发/重构完成后，逐项对照第三章维度与第四章清单打勾；任一维度不达标即视为该模块未完成质量门禁。

***

## 二、模块一基线概览（实例对照）

修复后的模块一达成了以下量化指标，作为其它模块的**下限参考**（不是上限，鼓励超越）：

| 维度                  | 模块一实例值 | 说明                                      |
| ------------------- | ------ | --------------------------------------- |
| 知识点数量               | **26** | order 1–26 连续无跳号                        |
| 面试题数量               | **16** | 覆盖文档流/盒模型/表单验证/meta/无障碍/语义化/srcset/场景题等 |
| 小测题数量               | **15** | 含【记忆】【理解】【应用】【对比】【场景】【综合】梯度标注           |
| 综合实战项目              | **2**  | KP22 搭建博客文章页、KP23 带验证注册表单               |
| 沙盒断言 checks         | **17** | 9 + 8，分布在 2 个实战项目沙盒                     |
| 知识点速查表              | **1**  | KP25                                    |
| 类型检查 `tsc --noEmit` | **通过** | exit 0                                  |
| 模块测试                | **通过** | html-fundamentals.test.ts 全绿            |

**关键文件锚点**：

- 模块数据：[src/modules/html-fundamentals.ts](../src/modules/html-fundamentals.ts)
- 沙盒类型：[src/lib/types.ts](../src/lib/types.ts) `SandboxData` / `SandboxCheck`
- 沙盒组件：[src/components/interactive/Sandbox.tsx](../src/components/interactive/Sandbox.tsx)
- 模块测试：[src/test/html-fundamentals.test.ts](../src/test/html-fundamentals.test.ts)

***

## 三、自查维度

每个维度包含：**① 标准要求 → ② 模块一实例 → ③ 检查方法 → ④ 常见缺陷**。

### 维度 A：知识点结构与编排

**① 标准要求**

- A1. `points` 数组的 `order` 字段从 1 连续递增至 N，**无重复、无跳号、无小数**。
- A2. 每个知识点具备 `title` / `difficulty`(1–5) / `blocks` 三要素。
- A3. 难度分布合理：以 1–3 为主，进阶内容(4–5)不超过总量的 30%。
- A4. 新增/重构的知识点显式标注 `isNew: true`，便于学习者识别更新。
- A5. `blocks` 内 `id` 遵循 `p{order}-{n}` 命名规范，全模块唯一。

**② 模块一实例**

26 个知识点 order 1–26 严格连续；KP22/KP23 实战项目标注 `isNew: true`；难度集中在 1–2，仅个别 API 章节为 3。

**③ 检查方法**

```bash
# 提取所有 order 值，人工核对连续性
# （用 IDE 正则搜索 ^\s+order:\s+(\d+),）
```

或在模块测试中断言：

```ts
it('知识点序号应从 1 到 N 连续', () => {
  const orders = someModule.points.map((p) => p.order)
  for (let i = 0; i < orders.length; i++) {
    expect(orders[i]).toBe(i + 1)
  }
})
```

**④ 常见缺陷**

- 插入新知识点后未重排后续 order，导致跳号/重复（模块一曾出现 order 21 / 21.5 / 22 冲突）。
- 用小数 order（如 21.5）作为临时插入，破坏连续性约定。

***

### 维度 B：评估环节有效性（以测促学）

**① 标准要求**

- B1. **面试题 ≥ 30 题**，覆盖该模块所有核心考点，无高频考点遗漏。
- B2. **小测题 ≥ 20 题**，且分布在模块末尾的独立 QuizCard 知识点。
- B3. 题目具备**梯度**，能区分"记忆 / 理解 / 应用"三个层次：
  - 记忆层：直问定义、标签类型；
  - 理解层：概念辨析、原理说明；
  - 应用层：场景判断、代码选型、排错。
- B4. 面试题至少包含 **2 道场景题**（"遇到 X 问题你怎么排查/重构"）与 **2 道对比题**（易混淆点 A vs B）。
- B5. 每道题的 `explanation` / `content` 不仅给结论，还解释"为什么"，指向易错点。
- B6. 考点覆盖率自查：列出模块所有核心知识点，逐个标注是否有对应题目，无盲区。

**② 模块一实例**

- 面试题 16 题：Q14 场景题（页面卡顿排查）、Q15 场景题（div 导航重构）、Q8 对比题（标准/IE 盒模型）、Q16 对比题（`<b>`/`<strong>` 等）。
- 小测题 15 题，题干前缀标注【理解】【应用】【对比】【场景】【综合】。
- 考点覆盖：文档流、盒模型、meta、viewport、语义化、defer/async、无障碍 POUR、ARIA、表单验证、srcset、空元素等均有对应题目。

**③ 检查方法**

```bash
# 统计面试题数量（匹配 Q{n}: 前缀）
# 统计小测题数量（匹配 ^\s+question:）
# 人工核对：是否存在"学了但没考"的知识点
```

**④ 常见缺陷**

- 题目全是直问定义，无法区分背书与理解（缺应用层）。
- 高频考点（如模块一的"文档流""表单验证"）无对应题目。
- 题目集中在模块末尾，章节学完无即时检验（理想态：每章末可加微测验，至少模块级总测验要全覆盖）。

***

### 维度 C：动态反馈闭环（沙盒断言）

**① 标准要求**

- C1. `SandboxData` 类型支持 `checks?: SandboxCheck[]` 字段（已由 types.ts 全局提供，各模块直接使用）。
- C2. `Sandbox.tsx` 渲染"任务检查清单"面板，对学习者代码**实时**跑断言并显示通过/未通过 + 中文教学提示（已由组件全局提供）。
- C3. 模块内**任务型练习沙盒**（尤其是实战项目）必须配置 `checks`，至少 5 条。**自由探索型沙盒**（hint 为"修改 X 观察 Y"的演练场）可不配 checks，因其教学目的是自由试错而非任务达标。区分依据：沙盒是否设定了明确的"完成目标"。
- C4. 每条 check 三要素齐全：`description`（任务要求）/ `pattern`（正则）/ `hint`（未通过时的教学引导）。
- C5. `pattern` 必须在沙盒中可正确编译（避免正则语法错误导致永远不通过）。
- C6. 断言不只是"语法存在性"检查，应体现**教学意图**（如"必须写 alt"对应无障碍教学点）。
- C7. 全部通过时显示完成态，给学习者正反馈。

**② 模块一实例**

- KP22 博客文章页沙盒：9 条 checks（charset / viewport / header / nav / main / article / img+alt / aside / footer）。
- KP23 注册表单沙盒：8 条 checks（form / text+required / email+required / password+minlength / select / button\[type=submit] / label\[for] / aria-required）。
- 每条 hint 指出"该怎么做 + 为什么"，例如 img+alt 的 hint 解释"无 alt 的图片对视障用户不存在，违反 WCAG 可感知原则"。

**③ 检查方法**

```bash
# 统计 checks 数量（匹配 ^\s+pattern:）
# 人工核对：每个配置了 checks 的沙盒，其 pattern 是否能在预期代码上通过、在错误代码上失败
```

**类型字段速查**：

```ts
export interface SandboxCheck {
  description: string  // 任务要求（展示给学习者）
  pattern: string      // 正则字符串，命中即通过
  flags?: string       // 默认 'i'
  hint: string         // 未通过时的教学提示
}
```

**④ 常见缺陷**

- 沙盒只支持"编辑-预览"，写错只能看浏览器原生报错，无教学反馈（模块一修复前的状态）。
- `pattern` 写得过严或过松：过严导致正确写法被判失败，过松导致随便写都通过。
- 只有语法检查没有语义检查（如只查"有没有 `<img>`"而不查"有没有 alt"）。

***

### 维度 D：任务导向内容组织（综合实战项目）

**① 标准要求**

- D1. 模块内至少 **1 个**（建议 2 个）综合实战知识点，放在该模块核心章节学完之后。
- D2. 实战项目必须**贯穿多个章节**的知识点，扮演"拼装图纸"角色，而非单点练习。
- D3. 每个实战项目配 `checks` 断言沙盒（接入维度 C）。
- D4. 沙盒提供**初始骨架代码**（带注释占位），降低冷启动门槛。
- D5. 项目前后有 `callout` 说明"为什么这个练习重要"与"实战后的反思"，连接工程思维。
- D6. 项目命名体现**真实开发场景**（如"搭建博客文章页""带验证的注册表单"），而非"练习 1"。

**② 模块一实例**

- KP22「搭建博客文章页」：串联 head 元信息 + 语义化骨架 + 多媒体无障碍，9 条断言，含 `callout variant="tip"` 说明练习价值。
- KP23「带验证注册表单」：串联表单控件 + 原生验证 + 无障碍 label 关联，8 条断言，含 `callout variant="warning"` 反思"前端验证的边界"（衔接后端校验）。
- 两者均提供带 `<!-- -->` 占位注释的初始骨架。

**③ 检查方法**

```bash
# 搜索实战项目知识点（title 含"实战"/"综合"）
# 核对每个实战沙盒是否配置 checks、是否有初始骨架、是否有 callout
```

**④ 常见缺陷**

- 知识按学科分类（结构→语义→多媒体→表单→API），学完仍不会"从零搭页面"。
- 没有贯穿多章的渐进式项目，知识点是离散积木缺拼装图纸。
- 实战项目变成"照抄示例"，无断言引导，无法定位薄弱环节。

***

### 维度 E：辅助学习资源

**① 标准要求**

- E1. 模块末尾含 1 个**知识点速查表**（`visualizationType: 'cheatsheet'` 或等价表格），浓缩核心 API/标签/属性。
- E2. 关键概念处使用 `callout`（`tip` / `warning` / `info`）强调易错点、最佳实践、边界条件。
- E3. 首个知识点有 `lead: true` 的引导段落，说明本章学什么、为什么学。
- E4. 代码示例遵守项目设计规范（canvas-mid 代码区、hairline 边框、等宽字体）。

**② 模块一实例**

- KP25 知识点速查表。
- 多处 callout：如表单章节的"无障碍表单要点"(tip)、KP23 的"前端验证边界"(warning)。
- 每章首段 `lead: true` 引导。

**③ 检查方法**

```bash
# 搜索 callout 使用密度（匹配 type: 'callout'）
# 确认存在 cheatsheet / 速查表知识点
```

**④ 常见缺陷**

- 全文平铺直叙，无重点强调，学习者抓不住易错点。
- 缺速查表，复习时无法快速定位。

***

### 维度 F：类型与工程质量

**① 标准要求**

- F1. `npx tsc --noEmit` 通过（exit 0），无类型错误。
- F2. 存在对应模块测试 `src/test/{module}.test.ts`，且全绿。
- F3. 测试覆盖：知识点数量、order 连续性、每个知识点三要素、可视化组件元数据。
- F4. 模块数据严格符合 `ModuleData` 类型（`points` / `blocks` / `data` 结构正确）。
- F5. 新增 `visualizationType` 时同步登记到 `visualizationMeta`（避免 types.test.ts 漂移）。
- F6. 不引入与模块无关的全局类型变更（避免污染其它模块）。

**② 模块一实例**

- `tsc --noEmit` exit 0。
- `html-fundamentals.test.ts` 14/14 通过（含 26 知识点、order 连续、三要素校验）。
- 注：`types.test.ts` 的 `visualizationMeta` (105 vs 173) 失败为**预先存在**的全局漂移，与模块一无关，需单独治理。

**③ 检查方法**

```bash
npx tsc --noEmit
npx vitest run src/test/{module}.test.ts --reporter=dot
```

**④ 常见缺陷**

- 新增知识点后忘更新测试中的 `toHaveLength(N)`，导致测试红。
- 新增可视化类型未登记 `visualizationMeta`，触发 types.test.ts 失败。
- order 用小数临时插入导致连续性测试失败。

***

## 四、自查清单（勾选式）

> 模块开发/重构完成后，逐项打勾。**任一未勾选 = 未过质量门禁。**

### A. 知识点结构

- [ ] A1. `order` 1..N 连续无重复无跳号无小数
- [ ] A2. 每个知识点有 `title` / `difficulty` / `blocks`
- [ ] A3. 难度分布合理（1–3 为主）
- [ ] A4. 新增知识点标注 `isNew: true`
- [ ] A5. block `id` 符合 `p{order}-{n}` 且全模块唯一

### B. 评估环节

- [ ] B1. 面试题 ≥ 30 题
- [ ] B2. 小测题 ≥ 20 题
- [ ] B3. 题目含记忆/理解/应用三梯度
- [ ] B4. 面试题含 ≥ 2 场景题 + ≥ 2 对比题
- [ ] B5. 每题 `explanation` 解释"为什么"
- [ ] B6. 核心考点全覆盖（无"学了没考"盲区）

### C. 动态反馈闭环

- [ ] C1. 使用 `SandboxData.checks`（类型已就绪）
- [ ] C2. 关键沙盒配置 `checks`（≥ 5 条）
- [ ] C3. 每条 check 三要素齐全（description/pattern/hint）
- [ ] C4. `pattern` 经实测：正确代码通过、错误代码失败
- [ ] C5. 断言体现教学意图（非纯语法检查）
- [ ] C6. 全部通过有完成态反馈

### D. 任务导向实战

- [ ] D1. 至少 1 个综合实战知识点（建议 2 个）
- [ ] D2. 项目贯穿多章节知识点
- [ ] D3. 配 `checks` 断言沙盒
- [ ] D4. 提供带注释占位的初始骨架
- [ ] D5. 前后有 `callout`（练习价值 + 反思）
- [ ] D6. 命名体现真实开发场景

### E. 辅助资源

- [ ] E1. 模块末尾有知识点速查表
- [ ] E2. 关键概念有 `callout` 强调
- [ ] E3. 首知识点有 `lead: true` 引导段
- [ ] E4. 代码示例符合设计规范

### F. 工程质量

- [ ] F1. `tsc --noEmit` 通过
- [ ] F2. 模块测试存在且全绿
- [ ] F3. 测试覆盖数量/order/三要素
- [ ] F4. 数据符合 `ModuleData` 类型
- [ ] F5. 新增 `visualizationType` 已登记 `visualizationMeta`
- [ ] F6. 未引入无关全局类型变更
- [ ] F7. 沙盒 `language` 值在 `SandboxData.language` 类型联合内（`'js' | 'html' | 'css' | 'typescript'`），新增语言须同步扩展类型联合（避免对象字面量多余属性报错）
- [ ] F8. 可视化组件多样性：全模块使用 ≥ 8 种不同 `visualizationType`（避免单一组件堆砌，提升学习体验）
- [ ] F9. 测试断言阈值与标准一致：模块测试中的数量/比例断言（面试题 ≥ 30、小测题 ≥ 20、checks ≥ 5、进阶占比 ≤ 30%、场景题 ≥ 2、对比题 ≥ 2）须与第四章清单保持一致，避免"标准说 30、测试只测 12"的阈值漂移

***

## 五、应用流程（如何把本标准用到新模块）

1. **新建模块骨架**：复制 `html-fundamentals.ts` 的结构（顶部 import、`ModuleData` 形状），改 `number/title/slug/stage/icon`。
2. **编写知识点**：按 `order` 1..N 顺序编写，遵循 A 系列标准。
3. **穿插实战项目**：在核心章节后插入综合实战 KP，配置 `checks` 沙盒（D + C 标准）。
4. **编写评估题**：先列出全部核心考点，再为每个考点配题，确保 B6 无盲区；题目前缀标注梯度。
5. **补速查表与 callout**：模块末尾加速查表，关键处加 callout（E 标准）。
6. **编写模块测试**：参照 `html-fundamentals.test.ts`，覆盖数量/order 连续/三要素。测试中的数量/比例断言须与第四章清单阈值一致（F9），避免"标准说 30、测试只测 12"。
7. **跑质量门禁**：
   ```bash
   npx tsc --noEmit
   npx vitest run src/test/{module}.test.ts --reporter=dot
   ```
8. **对照第四章清单逐项打勾**，未勾项回到对应维度修复。
9. **登记可视化类型**：若引入新 `visualizationType`，同步更新 `visualizationMeta` 与 `types.test.ts`。

***

## 六、模块四检查实录（DOM / BOM / Web API）

> 本次检查依据第三章 6 个自查维度 + 第四章勾选清单，对模块四（`dom-bom-webapi.ts`）逐项核验，对不符合项进行针对性修复，并补充测试断言以固化标准。

### 1. 检查范围与文件锚点

| 项目         | 锚点                                                                  |
| ---------- | ------------------------------------------------------------------- |
| 模块数据       | [src/modules/dom-bom-webapi.ts](../src/modules/dom-bom-webapi.ts)   |
| 沙盒类型       | [src/lib/types.ts](../src/lib/types.ts) `SandboxData` / `SandboxCheck` |
| 模块测试       | [src/test/dom-bom-webapi.test.ts](../src/test/dom-bom-webapi.test.ts) |
| 知识点数 / 可视化 | 25 / 23                                                              |

### 2. 不符合项清单

| 编号  | 维度    | 不符合项                                       | 原状                                       | 标准要求                          |
| --- | ----- | ----------------------------------------- | ---------------------------------------- | ----------------------------- |
| 1   | B1    | 面试题仅 16 题（Q1–Q16）                         | 题量不足，未覆盖防抖/节流、各类 Observer、postMessage、Web Worker、Clipboard、Page Visibility 等高频考点 | 面试题 ≥ 30 题（B1）               |
| 2   | A3    | 难度 4 的知识点 9 个，占比 9/25 = 36%              | 事件流/事件委托等基础概念被标为 4                       | 进阶内容(4–5) 不超过总量 30%（A3）       |
| 3   | A4    | KP25（小测验）未标注 `isNew: true`                | 重构后未补标                                   | 新增/重构知识点显式标注 `isNew: true`（A4） |

### 3. 修复措施

**① B1：面试题由 16 扩充至 30（新增 Q17–Q30，共 14 题）**

新增题覆盖以下高频考点，每题 `content` 均解释"原理 + 使用 + 易错点"：

- Q17 防抖与节流原理与实现
- Q18 IntersectionObserver 作用与使用
- Q19 ResizeObserver 作用与使用
- Q20 postMessage 跨源通信机制
- Q21 Web Worker 使用场景与限制
- Q22 requestIdleCallback 作用与使用
- Q23 拖拽 API（Drag and Drop）与 dataTransfer
- Q24 Clipboard API 剪贴板操作
- Q25 Page Visibility API 与后台资源管理
- Q26 【场景题】实现图片懒加载的方案对比
- Q27 【场景题】万级长列表渲染卡顿优化方案
- Q28 【对比题】scroll 事件监听 vs IntersectionObserver
- Q29 【对比题】防抖(debounce) vs 节流(throttle)
- Q30 【对比题】localStorage 跨标签通信 vs BroadcastChannel

修复后场景题 4 道（Q13/Q14/Q26/Q27）、对比题 5 道（Q15/Q16/Q28/Q29/Q30），均超过 B4 的"≥ 2 场景 + ≥ 2 对比"下限。

**② A3：KP7「事件流三阶段」、KP8「事件委托」难度 4 → 3**

这两项属基础概念（事件系统入门），调至 3 更符合认知梯度。修复后难度分布：

| 难度 | 1 | 2 | 3 | 4 | 5 |
| -- | - | - | - | - | - |
| 数量 | 2 | 4 | 12 | 7 | 0 |

进阶(4–5) 占比：7/25 = **28%** ≤ 30%，达标。

**③ A4：KP25 补 `isNew: true`**

KP25 小测验在重构时由 15 题扩至 20 题，属重构知识点，补标 `isNew: true`。

### 4. 测试断言同步强化

为防止后续回归，在 [dom-bom-webapi.test.ts](../src/test/dom-bom-webapi.test.ts) 中收紧以下断言：

| 测试用例                        | 原断言                       | 新断言                                   |
| --------------------------- | ------------------------- | -------------------------------------- |
| 面试题题量                       | `items.length >= 12`      | `items.length >= 30`（对齐 B1）            |
| 场景题/对比题                     | 仅匹配字符串存在                   | `场景题 >= 2` 且 `对比题 >= 2`（计数校验，对齐 B4）   |
| 难度分布                        | 仅校验范围 1–5 与 ≥ 3 种         | 新增 `进阶占比 <= 0.3`（对齐 A3）                 |

### 5. 复测结果

```bash
npx tsc --noEmit        # exit 0  通过
npx vitest run src/test/dom-bom-webapi.test.ts --reporter=dot
# Test Files  1 passed (1)
#      Tests  15 passed (15)
```

### 6. 自查清单勾选（模块四）

#### A. 知识点结构

- [x] A1. `order` 1..25 连续无重复无跳号无小数
- [x] A2. 每个知识点有 `title` / `difficulty` / `blocks`
- [x] A3. 难度分布合理（进阶 4–5 占 28% ≤ 30%）
- [x] A4. 新增/重构知识点标注 `isNew: true`（KP21/KP22/KP23/KP24/KP25）
- [x] A5. block `id` 符合 `p{order}-{n}` 且全模块唯一

#### B. 评估环节

- [x] B1. 面试题 ≥ 30 题（实际 30）
- [x] B2. 小测题 ≥ 20 题（实际 20）
- [x] B3. 题目含记忆/理解/应用三梯度（小测前缀【记忆】【理解】【应用】【对比】【场景】【综合】）
- [x] B4. 面试题含 ≥ 2 场景题 + ≥ 2 对比题（实际 4 场景 + 5 对比）
- [x] B5. 每题 `explanation` / `content` 解释"为什么"
- [x] B6. 核心考点全覆盖（事件/节点/存储/路由/几何/文件/调度/Observer/通信/拖拽/剪贴板）

#### C. 动态反馈闭环

- [x] C1. 使用 `SandboxData.checks`（类型已就绪）
- [x] C2. 关键沙盒配置 `checks`（KP21、KP22 各 6 条 ≥ 5）
- [x] C3. 每条 check 三要素齐全（description/pattern/hint）
- [x] C4. `pattern` 经实测：正确代码通过、错误代码失败
- [x] C5. 断言体现教学意图（如事件委托必须用 closest 定位、路由必须处理 404）
- [x] C6. 全部通过有完成态反馈（Sandbox.tsx 渲染任务清单）

#### D. 任务导向实战

- [x] D1. 至少 1 个综合实战知识点（实际 2：KP21、KP22）
- [x] D2. 项目贯穿多章节知识点（事件流+委托+节点操作 / history+popstate+路由表）
- [x] D3. 配 `checks` 断言沙盒（各 6 条）
- [x] D4. 提供带 `// TODO` 占位注释的初始骨架
- [x] D5. 前后有 `callout`（tip 练习价值 + warning 反思边界）
- [x] D6. 命名体现真实开发场景（"事件委托动态待办列表" / "手写简易 History SPA 路由"）

#### E. 辅助资源

- [x] E1. 模块末尾有知识点速查表（KP24，21 行）
- [x] E2. 关键概念有 `callout` 强调（tip/warning 多处）
- [x] E3. 首知识点有 `lead: true` 引导段（每个知识点首段均 lead）
- [x] E4. 代码示例符合设计规范（code 块含 language/filename）

#### F. 工程质量

- [x] F1. `tsc --noEmit` 通过（exit 0）
- [x] F2. 模块测试存在且全绿（15/15）
- [x] F3. 测试覆盖数量/order 连续/三要素/难度分布
- [x] F4. 数据符合 `ModuleData` 类型
- [x] F5. 新增 `visualizationType` 已登记 `visualizationMeta`（本模块未新增类型）
- [x] F6. 未引入无关全局类型变更
- [x] F7. 沙盒 `language` 值为 `'js'`，在类型联合内
- [x] F8. 可视化组件多样性 ≥ 8 种（实际 13 种）
- [x] F9. 测试断言阈值与标准一致（面试题 ≥ 30、小测题 ≥ 20、checks ≥ 5、进阶占比 ≤ 30%、场景题 ≥ 2、对比题 ≥ 2 均已固化为断言）

### 7. 关键决策说明

1. **面试题扩至 30 而非更高**：B1 下限为 30，30 已覆盖全部核心考点且无重复冗余，避免为凑数稀释质量；场景题/对比题分别达 4/5，远超下限。
2. **KP7/KP8 难度降为 3 而非其他**：事件流与事件委托是事件系统入门概念，属"理解层"而非"进阶层"；保留 KP12(history)/KP15(Cookie)/KP20(rAF) 等真正进阶内容为 4。
3. **未新增可视化类型**：模块已有 13 种可视化组件，无需引入新类型，避免触发 F5 漂移。
4. **测试断言同步收紧**：将 B1(≥30)、B4(场景/对比计数)、A3(进阶占比) 固化为测试，防止后续回归。

***

## 七、模块六检查实录（CSS 工程化与样式方案）

> 本次检查依据第三章 6 个自查维度 + 第四章勾选清单，对模块六（`css-engineering.ts`）逐项核验，对不符合项进行针对性修复，并补充模块测试以固化标准。模块六原为"信息型"模块（17 知识点、8 道小测、无面试/速查/沙盒断言），与评估、实战、反馈闭环标准差距较大，本次为系统补全。

### 1. 检查范围与文件锚点

| 项目         | 锚点                                                                  |
| ---------- | ------------------------------------------------------------------- |
| 模块数据       | [src/modules/css-engineering.ts](../src/modules/css-engineering.ts)   |
| 模块测试       | [src/test/css-engineering.test.ts](../src/test/css-engineering.test.ts) |
| 知识点数 / 可视化 | 21 / 8 种                                                            |

### 2. 不符合项清单

| 编号  | 维度        | 不符合项                                              | 原状                              | 标准要求                          |
| --- | --------- | ------------------------------------------------ | ------------------------------- | ----------------------------- |
| 1   | B1        | 无面试题知识点                                          | 模块仅含正文，缺面试评估                    | 面试题 ≥ 30 题（B1）               |
| 2   | B2/B3     | 小测仅 8 题、无梯度标注                                    | 8 题、无前缀                         | 小测题 ≥ 20 题，含记忆/理解/应用三梯度（B2/B3） |
| 3   | B4        | 无场景题/对比题                                         | 题量不足                            | ≥ 2 场景 + ≥ 2 对比（B4）          |
| 4   | C2        | 无任何带 `checks` 断言的沙盒                              | 沙盒仅纯演示（无 checks）                 | 实战沙盒 ≥ 5 条断言（C2）             |
| 5   | D1-D6     | 无综合实战知识点                                         | 缺实战项目                           | ≥ 1 个综合实战知识点（D1）             |
| 6   | E1        | 无知识点速查表                                          | 模块末尾无速查表                        | 模块末尾速查表（E1）                  |
| 7   | F2        | 无模块测试文件                                          | 未建立 css-engineering.test.ts     | 模块测试存在且全绿（F2）                |
| 8   | F8        | 可视化组件仅 7 种                                       | 不满足多样性                          | ≥ 8 种（F8）                    |

### 3. 修复措施

**① 知识点结构由 17 扩至 21（新增 KP17–KP21，重排测验位置）**

| 新 KP | 内容                | 类型            |
| ---- | ----------------- | ------------- |
| KP17 | BEM 命名重构卡片组件（实战）  | sandbox + checks |
| KP18 | Tailwind 响应式导航栏（实战） | sandbox + checks |
| KP19 | 面试题（30 题）         | accordion     |
| KP20 | 知识点速查表（21 行）      | table         |
| KP21 | 小测（20 题，含三梯度）     | quiz          |

原 KP17（测验）迁移并扩充为 KP21，KP16（知识图谱）补 `knowledgegraph` 可视化（成为第 8 种可视化类型，满足 F8）。

**② B1：新增面试题 30 题（KP19，Q1–Q30）**

覆盖 CSS 工程化全部核心考点：发展历程、Sass/CSS 变量、@mixin/@extend、Tailwind 理念/断点/@apply、CSS Modules/composes、CSS-in-JS 运行时 vs 零运行时、PostCSS、BEM、ITCSS/SMACSS、容器查询、@layer、主题切换、作用域隔离、CSS 性能、will-change、Houdini、:has()、调试技巧、优先级规则、渲染阻塞、SEO/无障碍、设计系统实现等。含 **2 道场景题**（Q26 Sass→Tailwind 迁移、Q27 首屏 LCP 优化）+ **3 道对比题**（Q28 CSS Modules vs CSS-in-JS、Q29 Tailwind vs BEM+Sass、Q30 @media vs @container），均达 B4 下限。

**③ B2/B3：小测由 8 扩至 20 题，加梯度前缀**

20 题分布：记忆 8 题（基础概念）、理解 6 题（机制辨析）、应用 4 题（选型决策）、对比 2 题（横向对照），覆盖 Sass/Tailwind/Modules/CSS-in-JS/PostCSS/BEM/ITCSS/@layer/@container/CSS 变量等。每题前缀 `【记忆】/【理解】/【应用】/【对比】`，满足 B3 三梯度。

**④ C/D：新增 2 个带 checks 的综合实战沙盒**

- **KP17 BEM 卡片组件**：6 条 checks（.card 块、__元素命名、--修饰符、CSS 变量定义、修饰符 var 覆盖、HTML 类名组合），含 tip（练习价值）+ warning（BEM 边界反思）。
- **KP18 Tailwind 响应式导航栏**：6 条 checks（md: 断点、flex-col 移动优先、hover 变体、dark 变体、flex 布局组合、语义化标签），含 tip + warning（原子化取舍）。

两沙盒均含 `initialCode` 骨架 + `TODO` 引导 + `checks` ≥ 5 + 前后 callout，满足 C2–C6、D1–D6。

**⑤ E1：新增知识点速查表（KP20，21 行）**

涵盖 Sass/CSS 变量、嵌套、Mixin/extend、BEM、Tailwind 断点/变体/@apply、CSS Modules、CSS-in-JS、Zero-Runtime、PostCSS、autoprefixer、@container、@layer、:has()、CSS 变量主题、composes、prefers-color-scheme、contain 等核心语法。

**⑥ F2：新建 [css-engineering.test.ts](../src/test/css-engineering.test.ts)**

15 个测试用例，覆盖元数据、21 知识点数量、order 连续、block id 唯一、可视化登记与多样性（≥8）、难度分布（进阶 ≤30%）、段落覆盖、小测 ≥20 + 三梯度、面试题 ≥30 + 场景/对比计数、速查表 ≥10 行、2 个 checks 沙盒 + 三要素、TODO 引导。CSS 沙盒语言为 `'html'`（内嵌 `<style>`），测试放宽为 `['html','js','css','typescript']`。

**⑦ F8：可视化多样性 7 → 8 种**

KP16「知识图谱」补 `knowledgegraph` 可视化（节点关系图展示 CSS 工程化各方案定位），成为第 8 种可视化类型。

### 4. 测试断言同步强化（F9）

为防止后续回归，[css-engineering.test.ts](../src/test/css-engineering.test.ts) 中将以下标准固化为断言：

| 测试用例       | 断言                                       | 对应标准          |
| ---------- | ---------------------------------------- | ------------- |
| 小测题量       | `questions.length >= 20`                 | B2            |
| 小测梯度       | 每题 `question` 匹配 `/^【.+】/` + 含三梯度         | B3            |
| 面试题题量      | `items.length >= 30`                     | B1            |
| 场景题/对比题    | `场景题 >= 2` 且 `对比题 >= 2`（计数校验）            | B4            |
| checks 沙盒  | `checks.length >= 5` + 三要素齐全             | C2/C3         |
| 速查表        | `rows.length >= 10` + 行列匹配               | E1            |
| 难度分布       | `进阶(4-5) / 总数 <= 0.3`                    | A3            |
| 可视化多样性     | `usedTypes.size >= 8`                    | F8            |

### 5. 复测结果

```bash
npx tsc --noEmit        # exit 0  通过
npx vitest run src/test/css-engineering.test.ts --reporter=dot
# Test Files  1 passed (1)
#      Tests  15 passed (15)
```

### 6. 自查清单勾选（模块六）

#### A. 知识点结构

- [x] A1. `order` 1..21 连续无重复无跳号无小数
- [x] A2. 每个知识点有 `title` / `difficulty` / `blocks`
- [x] A3. 难度分布合理（进阶 4–5 占比 ≤ 30%）
- [x] A4. 新增/重构知识点标注 `isNew: true`（KP16/KP17/KP18/KP19/KP20/KP21）
- [x] A5. block `id` 符合 `p{order}-{n}` 且全模块唯一

#### B. 评估环节

- [x] B1. 面试题 ≥ 30 题（实际 30）
- [x] B2. 小测题 ≥ 20 题（实际 20）
- [x] B3. 题目含记忆/理解/应用三梯度（小测前缀【记忆】【理解】【应用】【对比】）
- [x] B4. 面试题含 ≥ 2 场景题 + ≥ 2 对比题（实际 2 场景 + 3 对比）
- [x] B5. 每题 `explanation` / `content` 解释"为什么"
- [x] B6. 核心考点全覆盖（Sass/Tailwind/Modules/CSS-in-JS/PostCSS/BEM/ITCSS/@container/@layer/CSS 变量/:has()/Houdini）

#### C. 动态反馈闭环

- [x] C1. 使用 `SandboxData.checks`（类型已就绪）
- [x] C2. 关键沙盒配置 `checks`（KP17、KP18 各 6 条 ≥ 5）
- [x] C3. 每条 check 三要素齐全（description/pattern/hint）
- [x] C4. `pattern` 经实测：正确代码通过、错误代码失败
- [x] C5. 断言体现教学意图（BEM 必须 __ 元素 + -- 修饰符、Tailwind 必须 md: 断点 + dark: 变体）
- [x] C6. 全部通过有完成态反馈（Sandbox.tsx 渲染任务清单）

#### D. 任务导向实战

- [x] D1. 至少 1 个综合实战知识点（实际 2：KP17、KP18）
- [x] D2. 项目贯穿多章节知识点（BEM 命名 + CSS 变量 + 修饰符 / Tailwind 断点 + flex + 变体）
- [x] D3. 配 `checks` 断言沙盒（各 6 条）
- [x] D4. 提供带 `// TODO` 占位注释的初始骨架
- [x] D5. 前后有 `callout`（tip 练习价值 + warning 反思边界）
- [x] D6. 命名体现真实开发场景（"BEM 命名重构卡片组件" / "Tailwind 搭建响应式导航栏"）

#### E. 辅助资源

- [x] E1. 模块末尾有知识点速查表（KP20，21 行）
- [x] E2. 关键概念有 `callout` 强调（tip/warning/info 多处）
- [x] E3. 首知识点有 `lead: true` 引导段（每个知识点首段均 lead）
- [x] E4. 代码示例符合设计规范（code 块含 language/filename）

#### F. 工程质量

- [x] F1. `tsc --noEmit` 通过（exit 0）
- [x] F2. 模块测试存在且全绿（15/15）
- [x] F3. 测试覆盖数量/order 连续/三要素/难度分布
- [x] F4. 数据符合 `ModuleData` 类型
- [x] F5. 新增 `visualizationType` 已登记 `visualizationMeta`（本模块未新增类型，`knowledgegraph` 复用既有）
- [x] F6. 未引入无关全局类型变更
- [x] F7. 沙盒 `language` 值为 `'html'`，在类型联合内
- [x] F8. 可视化组件多样性 ≥ 8 种（实际 8 种：timeline/sandbox/codestepper/comparetable/architecture/knowledgegraph/accordion/quiz）
- [x] F9. 测试断言阈值与标准一致（面试题 ≥ 30、小测题 ≥ 20、checks ≥ 5、进阶占比 ≤ 30%、场景题 ≥ 2、对比题 ≥ 2 均已固化为断言）

### 7. 关键决策说明

1. **测验重排至末尾（KP21）而非保留原位置**：测验作为"评估闭环"应在面试题与速查表之后，形成"学→练→查→测"的认知流；避免测验穿插在正文与实战间打断节奏。
2. **CSS 实战沙盒语言用 `'html'` 而非 `'js'`**：CSS 工程化实战多为 `<style>` + HTML 结构，语言字段放宽为允许 `'html'`（已在 `SandboxData.language` 联合内），测试同步放宽断言。
3. **面试题 30 题而非更多**：B1 下限为 30，30 已覆盖全部核心考点（含 Houdini/:has()/优先级/渲染阻塞等进阶与边缘），场景/对比题达 2/3，避免冗余。
4. **KP16 补 knowledgegraph 而非新增类型**：`knowledgegraph` 已在 `visualizationMeta` 登记但模块六未使用，补上即可满足 F8 多样性，避免引入新类型触发 F5 漂移。
5. **测试断言同步收紧（F9）**：将 B1/B2/B3/B4/A3/C2/E1/F8 等阈值固化为测试，防止后续回归，与模块四做法一致。

